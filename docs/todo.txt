# Dotfiles Project TODO List

## P4: devx.sh Structural Refactoring (Blocked by pre-commit violations)

### Issue 1: Shebang Violation
**File**: shell-common/functions/devx.sh:1
**Current**: #!/bin/bash
**Required**: #!/bin/sh
**Reason**: Project standard for shell-common utilities

**Complexity**: HIGH
- File uses bash-specific features:
  - `local` keyword (line 225, 226, etc)
  - `BASH_SOURCE` array (line 56)
  - ${...} parameter expansion with bash semantics
  - Approx 50 lines need bash→POSIX conversion
- File already documents "Primarily for bash (uses local keyword, BASH_SOURCE, and other bash-isms)"

**Impact**: Medium
- Changes semantics of existing functions
- May break zsh compatibility despite current claims

**Status**: Deferred (requires careful refactoring)

---

### Issue 2: Library Purity Violation
**File**: shell-common/functions/devx.sh:364-366
**Problem**: Top-level main execution when sourced

```bash
# Current (problematic):
if [ "$_should_run_main" = "true" ]; then
    devx__main "$@"  # ← Executes during auto-sourcing
fi
```

**Required Fix**:
1. Remove main execution from shell-common/functions/devx.sh
2. Create shell-common/tools/custom/devx wrapper script
3. Update bash/main.bash devx loading logic

**Complexity**: HIGH
- Structural change affecting multiple files
- Changes devx command invocation pattern
- All dependent scripts may need updates
- Approx 100+ lines of code changes

**Impact**: High
- Breaks existing devx command flow
- Requires comprehensive testing

**Status**: Deferred (requires architectural review)

---

## Related Work

### LSP Audit Findings (P1-4)
**Priority**: Medium
**Date**: 2026-02-02

**Consistency Score**: 71% (2.12/3.0)

**Files Analyzed**:
- git.sh: 1.62/3.0 (missing help function, basic validation)
- devx.sh: 2.37/3.0 (good structure but pre-commit violations)
- mount.sh: 2.37/3.0 (comprehensive validation)

**Critical Issues Deferred**:
1. devx.sh exit code standardization (return 1 instead of return 2)
   - Cannot implement due to pre-commit blocking violations above
   - Blocked by Issue 1 + Issue 2 above

**Recommendations for Future Work**:
1. Prioritize devx.sh refactoring (Issues 1 + 2 above)
2. After that, implement exit code standardization
3. Then proceed with error message unified logging (Phase 2)
4. Finally, help system standardization and function naming

---

## Notes
- devx.sh is a "technical debt" file that was exempted from recent migrations
- Both violations are pre-commit BLOCKING (not just warnings)
- Recommend scheduling as separate major refactoring task
- Consider creating devx.sh v2 with proper architecture
