- 변경: **merge-train 대상 필터(draft / `reply-pending` / D-6 조용한 기간)를 `shell-common/functions/gh_pr_merge_train.sh` 한 곳으로 통합 — cron 디스패처의 `jq` 와 `gh:pr-merge-train` 스킬의 산문으로 이중 존재하던 규칙을 두 호출부가 같은 `_gh_pr_merge_train_filter_targets` 를 부르도록 바꿨고, 11분이라는 숫자도 `_gh_pr_merge_train_quiet_minutes`(env `GH_PR_MERGE_TRAIN_QUIET_MINUTES` 로 조정) 한 곳에만 남는다 (#1524)**
- 변경: **`reply-pending` 라벨 도입 — `devx:pr-review-all` 의 `defer` 브랜치가 붙이고 `gh:pr-reply` Step 6 이 무조건 제거하며, 라벨이 붙은 PR 은 경과 시간과 무관하게 merge-train 이 건너뛴다. 조용한 기간은 라벨이 없는 PR 과 라벨을 떼기 전에 죽은 세션을 위한 백스톱으로만 남는다 (#1524)**
