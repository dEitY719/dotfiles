- 변경: **merge-train 대상 필터(draft / `reply-pending` / D-6 조용한 기간)를 `shell-common/functions/gh_pr_merge_train.sh` 한 곳으로 통합 — cron 디스패처의 `jq` 와 `gh:pr-merge-train` 스킬의 산문으로 이중 존재하던 규칙을 두 호출부가 같은 `_gh_pr_merge_train_filter_targets` 를 부르도록 바꿨고, 11분이라는 숫자도 `_gh_pr_merge_train_quiet_minutes`(env `GH_PR_MERGE_TRAIN_QUIET_MINUTES` 로 조정) 한 곳에만 남는다 (#1524)**
- 변경: **`reply-pending` 라벨 도입 — `devx:pr-review-all` 의 `defer` 브랜치가 붙이고 `gh:pr-reply` 가 무조건 제거하며, 라벨이 붙은 PR 은 조용한 기간과 무관하게 merge-train 이 건너뛴다 (#1524)**
- 변경: **`reply-pending` 하드 스킵에 만료를 둔다 — `updatedAt` 이 `_gh_pr_merge_train_reply_pending_stale_minutes`(기본 90분, env `GH_PR_MERGE_TRAIN_REPLY_PENDING_STALE_MINUTES`) 를 넘으면 라벨을 스테일로 보고 평소의 조용한 기간 판정으로 넘긴다. 라벨을 떼기 전에 죽은 세션 때문에 PR 이 트레인에서 영구 제외되던 경로를 없앤 것이며, 이로써 문서가 말하던 백스톱이 실제로 성립한다 (#1524)**
- 변경: **`gh:pr-reply` 가 답할 코멘트가 없어 조기 종료할 때도 `reply-pending` 라벨을 제거한다 — 라벨 제거는 이제 모든 종료 경로에서 실행되고, 라벨이 애초에 없어 DELETE 가 404 를 돌려주는 통상 경로는 `[WARN]` 이 아니라 `[OK]` 로 보고해 실제 실패(인증/네트워크/5xx)만 경고로 남는다 (#1524)**
