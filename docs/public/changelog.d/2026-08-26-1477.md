- 변경: **`pr_merge_train_cron.bats` 의 herdr tab/agent-start 실패 테스트 2건에 양성 대조를 붙였다 (#1477).** `_run_tick` 이 대상 스크립트를 찾지 못해 bash 가 비영점 종료해도 `assert_failure` 와 `_refute_logged "herdr agent prompt"` 는 그대로 만족되므로, 디스패처가 통째로 사라져도 이 2건은 초록이었다 — PR #1476 머지 커밋의 신선한 클론 차등 검증에서, 41건 중 이 2건만 스크립트 부재 시에도 통과하며 실측됐다. herdr 스텁이 실패를 반환하기 전에 호출을 로그에 남기는 것을 확인해, 각 테스트에 `_assert_logged "herdr tab create"` / `_assert_logged "herdr agent start"` 를 한 줄씩 추가했다 — 형제 케이스(:516)가 이미 갖춘 음성+양성 형태로 맞춘 것이며, 같은 결함 클래스를 issue-watcher 스위트에서 고친 `7e05b473`, aicron 스위트에서 고친 #1479 와 동일한 근거다. 검증은 차등으로 했다: 스크립트를 치우면 종전엔 통과하던 이 2건이 지금은 실패하고, 원복하면 41/41 그대로 통과한다
