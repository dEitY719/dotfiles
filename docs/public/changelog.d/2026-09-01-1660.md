- 변경: **`dEitY719/gh-resolve-skills` 레포를 신규 생성하고 `gh-pr-resolve-ci-fail` · `gh-pr-resolve-conflict` · `gh-pr-resolve-outdated` 세 스킬을 이관했다 — #1410 스킬 마켓플레이스 분리 계획의 Phase 2. 이 레포가 맡는 축은 "PR 차단 상태 복구"(빨간 CI · 리베이스 충돌 · base 아웃데이트)다 (#1660)**
- 변경: **플러그인명 `gh-resolve` 가 네임스페이스를 공급하므로 스킬 디렉토리명에서 `gh-pr-resolve-` 접두를 제거했다 — 새 레포에서는 `/gh-resolve:ci-fail` · `/gh-resolve:conflict` · `/gh-resolve:outdated` 형태로 호출한다. dotfiles 의 `claude/skills/gh-pr-resolve-*` 원본은 그대로 남아 `/gh:pr-resolve-conflict` 계열 호출도 계속 동작한다 (#1410 NF-1, 삭제는 Phase 4)**
- 변경: **레포에 6개 하네스 매니페스트(Claude Code / Codex / Kimi / Hermes / OpenCode / Gemini·Antigravity)를 갖췄고, CI 는 처음부터 `harness-skills` 가 소유한 재사용 워크플로(`skill-check.yml@main`)를 `plugin-name: gh-resolve` 로 호출한다 — 검사 로직을 복제하지 않아 드리프트가 생기지 않는다 (#1410 D-10, NF-2)**
- 변경: **이관 과정에서 `conflict`(125줄) · `outdated`(130줄) SKILL.md 를 공용 CI 의 100줄 상한에 맞춰 progressive disclosure 로 재구성했다 — 삭제 없이 `references/constraints.md`, `references/step5-helpers.md`, `references/push-and-report.md` 로 분리하고 SKILL.md 에는 참조 지점만 남겼다. `--force-with-lease` 관련 안전장치 문구는 본문에 그대로 유지했다**
- 변경: **이관본의 ai-metrics footer 는 이모지 글리프 대신 평문으로 렌더한다 — 공용 CI 의 "No emojis in tracked text" 검사와 충돌하기 때문이다. dotfiles 원본의 이모지 예외(#317 F-2)는 그대로 유지되며, `<!-- ai-metrics -->` 마커 쌍과 수치는 양쪽이 동일하다**
- 변경: **`claude/plugin/marketplaces.json` 에 `gh-resolve-skills`, `claude/plugin/plugins.json` 에 `gh-resolve@gh-resolve-skills` 를 등록했다 — `claude/plugin/restore.sh` 가 새 항목을 복원 대상으로 인식한다**
