- 변경: **`dEitY719/gh-verify-skills` 레포를 신설하고 PR 검증 축의 5개 스킬(`devx-pr-review-all`, `devx-pr-verify-live`, `devx-pr-verify-merged`, `devx-exception-merge-checklist`, `gh-pr-post-merge-verify`)을 `gh-verify` 플러그인으로 이관했다 — #1410 스킬 마켓플레이스 분리 계획의 Phase 2. 새 레포에서는 plugin 명과 중복되는 접두가 빠져 `/gh-verify:review-all`, `/gh-verify:live`, `/gh-verify:merged`, `/gh-verify:exception-merge-checklist`, `/gh-verify:post-merge-verify` 형태로 호출한다 — dotfiles 의 기존 `claude/skills/` 원본은 그대로 남아 `/devx:pr-review-all` 등 기존 호출도 계속 동작한다(NF-1, 삭제는 Phase 4) (#1659)**
- 변경: **`gh-verify-skills` 의 CI 는 `harness-skills` 의 재사용 워크플로(#1410 D-10)를 처음부터 호출한다 — 검사 로직을 레포마다 복제하지 않는다(NF-2)**
- 변경: **`claude/plugin/marketplaces.json` 에 `gh-verify-skills`, `claude/plugin/plugins.json` 에 `gh-verify@gh-verify-skills` 를 등록했다 — `claude/plugin/restore.sh` 가 새 항목을 복원 대상으로 인식한다**
