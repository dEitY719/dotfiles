- 변경: **`gh:pr-merge-train` 의 `review-passed` 게이트가 이제 head sha 를 검증한다 — `devx_pr_review_all_apply_label` 이 라벨을 붙일 때 `<!-- review-verdict:review-passed:<head-sha> -->` 마커 코멘트를 함께 남기고, F-3 재조회 시점에 `_gh_pr_merge_train_review_passed_stale` 가 그 sha 를 현재 `headRefOid` 와 비교한다. 라벨의 기존 무효화 경로(`gh:pr-reply`/`gh:pr-resolve-conflict`/`gh:pr-resolve-outdated`/`devx:pr-review-all` Step 4)는 그 스킬들이 직접 push 할 때만 라벨을 지워, 수동 `git push`·GitHub 웹 UI 커밋처럼 그 경로 밖에서 head 가 전진하면 리뷰되지 않은 커밋에 `review-passed` 가 그대로 남는 구멍이 있었다(#1601). 마커 부재/불일치는 fail-closed 로 `[SKIPPED] review-passed label stale — head advanced without invalidation` 처리되고, 게이트가 스스로 발견한 stale 라벨은 그 자리에서 제거해 자가 치유한다.**
- 변경: **위 freshness 마커의 신뢰 기준을 텍스트 매치에서 작성자 검증으로 강화했다(PR #1608 리뷰, agy+codex 독립 BLOCKER) — 라벨 부착과 달리 PR 코멘트는 쓰기 권한 없이 누구나 남길 수 있어, 마커 문자열만 보던 최초 구현은 아무 참여자나 `review-passed` 를 손으로 위조해 stale 보호를 무력화할 수 있었다. `_gh_pr_merge_train_review_passed_marker_sha` 가 이제 파이프라인이 실제로 인증하는 계정(`gh api user -q .login`)이 남긴 마커만 신뢰하고, 그 외 계정의 마커는 위조로 간주해 무시한다.**
- 변경: **freshness 조회 자체가 실패했을 때 라벨을 삭제하지 않도록 고쳤다(PR #1608 리뷰, agy 2차 라운드 BLOCKER) — 이전 구현은 "마커가 확인상 없음"과 "네트워크/인증 오류로 조회 자체가 실패함"을 구분하지 못해, 일시적인 `gh api` 장애만으로도 멀쩡한 `review-passed` 라벨을 영구 삭제할 수 있었다. `_gh_pr_merge_train_review_passed_marker_sha`/`_gh_pr_merge_train_review_passed_stale` 가 이제 3단계 종료 코드(신선함/확인된 stale/판정불가)를 구분하고, F-3 재확인은 "확인된 stale"일 때만 라벨을 지우며 "판정불가"는 이번 tick만 skip하고 라벨은 그대로 둔다.**
