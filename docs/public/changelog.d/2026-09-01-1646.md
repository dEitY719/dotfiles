- 변경: **기존 `dEitY719/claude-plugin-visuals` 레포를 `dEitY719/visuals-skills` 로 rename 하고 mono-plugin(`plugins/visuals/skills/*`) 레이아웃을 single-plugin(`skills/*`) 레이아웃으로 전환했다 — #1410 스킬 마켓플레이스 분리 계획의 Phase 1. 신규 생성이 아닌 rename 이라 히스토리·스타·기존 클론 remote 가 GitHub 자동 리다이렉트로 보존된다 (#1646)**
- 변경: **`devx-md-to-scrolldeck` 스킬을 `visuals` 플러그인에 추가해 스킬이 3종(`visualize`, `excalidraw-diagram`, `md-to-scrolldeck`)이 됐다 — 새 레포에서는 접두사가 빠져 `/visuals:md-to-scrolldeck` 형태로 호출한다. dotfiles 의 `claude/skills/devx-md-to-scrolldeck/` 원본은 그대로 남아 `/devx:md-to-scrolldeck` 호출도 계속 동작한다 (NF-1, 삭제는 Phase 4)**
- 변경: **레포에 6개 하네스 매니페스트(Claude Code / Codex / Kimi / Hermes / OpenCode / Gemini·Antigravity)를 갖췄다 — rename 전에는 Claude Code 용 매니페스트만 있었다. 기존 `plugin.json` 의 `author`(원저작자) / `maintainer`(dEitY719) 분리 표기와 `license: MIT` 는 그대로 승계했다**
- 변경: **`visuals-skills` CI 는 `harness-skills` 의 재사용 워크플로(#1410 D-10) 대신 독립 workflow 로 시작한다 — 공용 워크플로의 "No emojis in tracked text" 검사가 이 레포와 구조적으로 충돌하기 때문이다. `visualize` 스킬의 예제 HTML(포스터·덱·인포그래픽) 15개 파일이 이모지를 의도된 디자인 글리프로 쓴다. 전환하려면 공용 워크플로에 `check-emojis` 같은 opt-out 입력이 먼저 필요하다**
- 변경: **`claude/plugin/marketplaces.json` 에 `visuals-skills`, `claude/plugin/plugins.json` 에 `visuals@visuals-skills` 를 등록했다 — `claude/plugin/restore.sh` 가 새 항목을 복원 대상으로 인식한다**
- 변경: **`ghes_mirror` 위저드의 기본 upstream URL 을 `claude-plugin-visuals` 에서 `visuals-skills` 로 갱신했다 (`shell-common/functions/ghes_mirror.sh`)**
