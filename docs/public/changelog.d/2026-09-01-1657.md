- 변경: **`dEitY719/spec-flow-skills` 레포를 신설하고 `devx-{prd-to-trd,trd-to-issues,pr-to-ssot-issue,reverse-engineering-analysis,claude-to-codex}` 5개 스킬을 `spec-flow` 플러그인으로 이관했다 — #1410 스킬 마켓플레이스 분리 계획의 Phase 2 첫 레포. PRD -> TRD -> 이슈 파이프라인 축을 맡는다. 새 레포에서는 디렉터리 접두사 `devx-` 가 빠져 `/spec-flow:prd-to-trd` 형태로 호출한다 — dotfiles 의 `claude/skills/devx-*` 원본은 그대로 남아 `/devx:prd-to-trd` 호출도 계속 동작한다(NF-1, 삭제는 Phase 4) (#1657)**
- 변경: **`spec-flow-skills` 의 CI 는 처음부터 `harness-skills` 의 재사용 워크플로(#1410 D-10)를 호출한다 — Phase 0/1 이 거쳤던 "독립 workflow 로 시작 후 전환" 임시 조치가 `harness-skills` 완료로 더 이상 필요 없기 때문이다 (#1657)**
- 변경: **`spec-flow-skills` CI 의 이모지 예외(`allow-emoji-paths`)는 파일 2개로 한정했다 — `pr-to-ssot-issue` 의 ai-metrics footer 템플릿(dotfiles #317 F-2 가 승인한 유일한 이모지 예외)과 `trd-to-issues` 가 인용하는 GitHub 우선순위 라벨명뿐이며, 둘 다 이모지 자체가 출력 대상이다. 디렉터리 단위로 넓히지 않았다 (#1657)**
- 변경: **`claude/plugin/marketplaces.json` 에 `spec-flow-skills`, `plugins.json` 에 `spec-flow@spec-flow-skills` 를 등록했다 — `claude/plugin/restore.sh` 가 새 항목을 복원 대상으로 인식한다 (#1657)**
