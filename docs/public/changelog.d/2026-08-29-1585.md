- 변경: **herdr 무인 디스패처 3곳의 prompt-stall 처리 가시성을 높였다** — `gh-pr-post-merge-verify` 와 `pr_merge_train_cron.sh` 는 `agent_prompt_stalled`/`timeout` 에서 최대 3회 재시도한 뒤 탭을 `*-STUCK` 으로 rename 하고 `herdr notification show` 로 attach 힌트를 띄운다. `issue_watcher_cron.sh` 는 기존 Enter 제출 복구를 유지하되 최종 실패 시 같은 식으로 tab rename + notification 에스컬레이션을 추가해, 사람이 화면을 보고 있지 않아도 멈춘 세션을 다음 점검 때 바로 찾을 수 있게 했다 (#1585)**
