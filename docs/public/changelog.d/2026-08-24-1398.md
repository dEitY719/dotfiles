- 변경: **`setup.sh` 가 온보딩 중 `tests/bats/lib/*`(bats-core/bats-support/bats-assert) git submodule 을 자동 초기화한다 — 신규 `shell-common/functions/dotfiles_bats_submodules.sh` 의 `dotfiles_ensure_bats_submodules()` 를 git-crypt 무력화 직후에 호출해 `git submodule update --init --recursive tests/bats/lib` 를 실행한다(git 저장소가 아닌 checkout 은 no-op, 실패는 경고만 하고 계속 진행). #1397 이 `run_bats()` 자체를 fail-closed 로 고쳤지만 온보딩 경로에서 submodule 을 채워주는 쪽은 다루지 않았는데, 그 gap 을 메운다 — `./setup.sh` 를 거친 신규 클론은 이제 bats-core 가 곧바로 준비되어 위 fail-closed 게이트에 걸리지 않는다 (#1398)**
