- 변경: **`dEitY719/authoring-skills` 레포를 신설하고 `skill-{create,check,refactor}`·`sh-check`·`devx-{ux-guidelines,command-rename}` 6개 스킬을 `authoring` 플러그인으로 이관했다 — #1410 스킬 마켓플레이스 분리 계획의 Phase 2. 새 레포에서는 `devx-` 접두사만 빠져 `/authoring:skill-check`·`/authoring:sh-check`·`/authoring:ux-guidelines`·`/authoring:command-rename` 형태로 호출한다 — dotfiles 의 기존 `claude/skills/` 원본 6개는 그대로 남아 `/skill:create`·`/sh:check`·`/devx:ux-guidelines` 호출도 계속 동작한다(NF-1, 삭제는 Phase 4) (#1662)**
- 변경: **`authoring-skills` CI 는 `harness-skills` 의 재사용 워크플로(#1410 D-10)를 처음부터 호출한다 — 검사 로직을 레포에 복제하지 않아 15-way 드리프트를 막는다(NF-2). `skill-check` 스킬의 `references/` 는 ai-metrics footer 이모지 정책 자체를 설명하는 문서라 `allow-emoji-paths` 로 그 한 경로만 예외 처리했다**
- 변경: **`claude/plugin/marketplaces.json` 에 `authoring-skills`, `claude/plugin/plugins.json` 에 `authoring@authoring-skills` 를 등록했다 — `claude/plugin/restore.sh` 가 새 항목을 복원 대상으로 인식한다**
