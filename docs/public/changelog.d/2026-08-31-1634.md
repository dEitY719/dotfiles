- 변경: **`gh:pr-reply`가 Step 5(코멘트 전원 답변)를 완수하면 `review-blocked`를 무조건 해제한다 — `PUSHED_FIXES`나 ACCEPT/DECLINE 비율과 무관하며, `review-passed`의 무조건 해제 규칙과 대칭이다. 리뷰어 자신의 BLOCKER 항목을 근거를 갖춰 정당하게 DECLINE한 경우에도 더 이상 라벨이 눌러앉지 않는다(PR #1630 사례)**
- 변경: **push가 있었을 때(`PUSHED_FIXES > 0`)만 도는 타겟 재검토 lane은 이제 `review-blocked` 해제 게이트가 아니라 `review-passed` 승격을 노리는 선택 경로다 — 재검토가 BLOCKING을 새로 보고하면 독립 근거로 `review-blocked`를 재적용한다**
