- 변경: **`gh:issue-flow` 가 PR 생성 직후(Step 2.4.5) merge-train dispatcher(`aicron run merge-train`)를 즉시 1회 호출하고, `merge-train` 크론 백스톱 주기를 `*/23` 에서 `*/5` 로 좁혔다 (#1482).** 종전에는 PR 이 완성돼도 다음 `*/23` 크론 tick 까지 최대 23분간 아무 것도 처리되지 않았다. 새 Step 2.4.5 는 `gh:pr-merge-train` 스킬을 직접 부르지 않고 기존 dispatcher 스크립트(`pr_merge_train_cron.sh`)만 재호출한다 — dispatcher 가 이미 갖고 있는 NF-1 이중 잠금(tick flock + `herdr agent get pmt-…` 라이브 감지)을 그대로 재사용해, 여러 issue-flow 세션이 근접한 시각에 완료돼도 병렬 train 이 생기지 않는다. 이 호출은 non-fatal(`|| true`)이며 Step 2.4 의 soft-fail 여부와 무관하게 실행된다 — merge-train 자체의 11분 정숙구간(D-6)이 리뷰 반영 대기를 이미 보장하므로, 이 스텝은 "지금 확인해봐"라는 신호일 뿐 머지 타이밍을 앞당기지 않는다. dispatcher 가 이미 `live` 상태면 조용히 스킵하고 재시도하지 않는 기존 설계상, 이벤트 트리거가 드롭된 경우를 잡아주는 유일한 안전망이 크론 주기라 `*/23` 을 `*/5` 로 단축했다. `gh:pr-approve` 를 통한 self-authored PR 자동 승인, `--admin-merge` 를 `gh:issue-flow` 에 넣는 방안은 검토 후 기각했다 — 전자는 GitHub 가 서버단에서 차단하는 동작이고, 후자는 이 저장소가 이미 승인 게이트 0(`required_approving_review_count=0`)이라 불필요하며 project-board Status 게이트까지 우회해 NF-2 를 위반한다.
