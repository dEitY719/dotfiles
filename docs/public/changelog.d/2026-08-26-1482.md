- 변경: **`gh:issue-flow` 가 PR 생성 직후(Step 2.4.5) merge-train dispatcher(`aicron run merge-train`)를 백그라운드로 즉시 1회 호출하고, `merge-train` 크론 백스톱 주기를 `*/23` 에서 `*/5` 로 좁혔다 (#1482).** 종전에는 PR 이 완성돼도 다음 크론 tick 까지 최대 23분간 아무 것도 처리되지 않았다. 새 Step 2.4.5 는 `gh:pr-merge-train` 스킬을 직접 부르지 않고 기존 dispatcher 스크립트만 재호출해 그 NF-1 이중 잠금(tick flock + `herdr agent get` 라이브 감지)을 그대로 재사용하며, dispatcher 의 최대 4분짜리 `herdr agent prompt --wait` 를 기다리지 않도록 백그라운드로 실행해 이후 rebase 스텝을 막지 않는다. 설계 근거·기각한 대안(self-authored 자동 승인, `--admin-merge`)은 `claude/skills/gh-issue-flow/references/merge-train-dispatch.md` 참고.
