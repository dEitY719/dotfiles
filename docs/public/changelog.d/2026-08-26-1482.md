- 변경: **`gh:issue-flow` 가 PR 생성 직후 merge-train dispatcher 를 즉시 1회 깨우고, 백스톱 크론 주기를 `*/23` 에서 `*/5` 로 단축했다.** 기존에는 `pr_merge_train_cron.sh` 가 자체 주기로 돌 때만 머지가 트리거돼 PR 완성 후 최대 23분간 유휴 시간이 생겼다. Step 2.4(`devx:pr-review-all`) 직후 같은 dispatcher 를 `aicron run merge-train` 으로 한 번 더 호출해 이 지연을 사실상 없앴고, dispatcher 는 non-fatal 로 실행돼 실패해도 체인을 막지 않는다. `gh:pr-merge-train`/`aicron` 자체의 NF-1 이중 잠금(flock + herdr agent name)은 그대로 유지돼 여러 세션이 동시에 완료돼도 train 이 중복 생성되지 않는다 (#1482)
- 변경: **`merge-train` 크론 주기 변경(`*/5`)은 이미 설치된 crontab 에 자동 반영되지 않는다 — 각 머신에서 최신 체크아웃으로 pull 한 뒤 `aicron remove merge-train && aicron add merge-train` 를 한 번 실행해야 적용된다 (#1482).**
