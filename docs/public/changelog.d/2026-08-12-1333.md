- 변경: **`/obsidian:resolve-conflict` Step 9 의 최종 출력 계약을 명시 — 성공은 반드시 `[OK] 해결 완료`, 중단은 `[FAIL] <reason>`, 보류 상태는 실행 가능한 후속 `/obsidian-resolve-conflict <side> --vault <vault>/90-personal` 명령으로 끝난다. `verify-sync.sh` 가 출력한 `FAIL` / `SUGGEST` 줄은 그대로 보여주며, `해결 완료` 단독 출력은 금지해 성공/실패 verdict 와 보류 후속 명령을 기계적으로 구분할 수 있게 했다 (#1333)**
