- 변경: **`gh:pr-resolve-ci-fail` 이 Step 5 push 성공 직후 스테일 `review-passed` 라벨을 뗀다** — head 를 전진시키는 다섯 스킬 중 이것만 판정 라벨을 아예 건드리지 않아, 리뷰된 커밋이 더 이상 head 가 아닌데도 판정이 살아남았다(`gh:pr-merge-train` 의 마커 stale 검사가 뒤늦게 잡아 줄 때까지 아무도 몰랐다). 삭제는 공유 헬퍼 `_gh_pr_drop_label` 로 가고 **무조건**이다 — CI 수정 커밋은 정의상 파일 내용을 바꾸므로 `gh:pr-resolve-outdated` / `gh:pr-resolve-conflict` 의 patch-id 동일 유지 경로(#1698 / #1700)가 여기서는 성립할 수 없다. push 가 거부되면 Step 7 자체가 실행되지 않아 라벨은 그대로 유효하고, 삭제 실패는 `[WARN]` 한 줄일 뿐 CI 복구 성공/실패를 바꾸지 않는다. `review-blocked` 는 절대 건드리지 않는다 — 리뷰 지적이 반영됐다는 증거가 이 스킬에는 없고, 두 판정 라벨의 발급자는 `devx:pr-review-all` 하나뿐이다 (#1563). 회귀 테스트는 `tests/bats/skills/gh_pr_resolve_ci_fail_review_passed.bats` (#1705)
