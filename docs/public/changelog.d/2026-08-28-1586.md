- 변경: **merge-train cron 주기를 `*/5`→`*/2` 로 낮춘다 — 실측(PR #1573/#1580, force-push 직후 필수 체크 완료까지 34~40초)이 `BLOCKED`(체크 진행 중)로 스킵된 PR이 다음 tick까지 최대 5분 가까이 대기하던 것을 보여줬다. idle tick 비용은 `_pmt_target_count` 의 `gh pr list` 1회 호출 + 즉시 종료로 무시할 수준이고, 진행 중인 train 과의 중복 실행은 `_pmt_train_state` 의 NF-1 singleton lock 이 이미 안전하게 막는다. `--help` 의 stale `*/15` crontab 예시도 새 값으로 맞췄다 — 이미 매니페스트 값(`*/5`)과도 어긋나 있던 기존 드리프트였다(#1579/#1580 이 issue-watcher 에서 잡은 것과 같은 종류). (#1586)**
- 변경: **운영자 조치: `merge-train` 잡을 재설치해야 새 주기가 적용된다 — `aicron remove merge-train && aicron add merge-train`. crontab 줄의 스케줄 자체는 설치 시점에 고정되므로, 매니페스트만 바꿔서는 실제 crontab 이 갱신되지 않는다. 확인은 `aicron doctor` 로 schedule drift 가 없는지 보면 된다. (#1586)**
