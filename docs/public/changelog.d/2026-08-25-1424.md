- 변경: **`gh:issue-create` 가 대화 맥락의 선행-이슈 문구를 감지해 새 이슈를 GitHub 네이티브 Issue Dependencies(`addBlockedBy`)로 자동 연결한다. 신규 Step 2.6(감지)이 `#N 완료/해결 + 후·뒤·되면·하고·하면` · `#N 이후` · `선행 이슈 #N`(콜론 선택) · `depends on #N` · `blocked by #N` 다섯 갈래만 매칭하고, `#N 참고`/`#N 관련` 같은 단순 언급은 의도적으로 제외한다 — 오탐 링크는 누락보다 나쁘기 때문이다(링크된 이슈는 dispatcher 관점에서 조용히 blocked 상태가 된다). 실제 mutation 은 신규 이슈 번호가 존재해야 하므로 Step 4(생성) 직후의 신규 Step 4.5 로 미뤄지고, 노드 id 두 개를 alias 로 한 번에 조회한 뒤 `addBlockedBy` 를 호출한다. 라벨(`blocked-by-13`) 방식은 `#13` 이 닫힐 때 누가 라벨을 지우느냐는 별도 동기화 문제를 낳고, 본문 트레일러(`Depends-on: #13`) 방식은 GitHub 이 이미 제공하는 조회 인터페이스를 재발명하는 셈이라 둘 다 기각했다 — 네이티브 의존성은 상태가 항상 실제 이슈 상태와 동기화되고 GH UI 사이드바에 `Blocked by #13` 으로 그대로 보인다. 링크 실패(권한/네트워크/없는 번호)는 non-fatal 이다: 이슈는 이미 만들어진 뒤이므로 중단·재시도 없이 리포트에 `[WARN] Blocked by #N 링크 실패 — GH UI에서 수동 추가 필요` 한 줄만 덧붙인다. `owner/repo#N` 형태의 cross-repo 참조는 v1 범위 밖이라 경고 후 스킵하는데, 참조 정규식이 `owner/repo` 접두사를 **선택적으로 포함**하도록 짜여 있어서 타 저장소 참조가 같은 번호의 자기 저장소 이슈로 오인되지 않는다. 탈출구는 `--no-auto-deps` (기존 `--no-auto-labels` 와 같은 패턴, `DISCUSSION_MODE=1` 일 때도 자동 스킵 — Discussion 에는 의존성 그래프가 없다). 감지 로직은 `tests/bats/skills/_fixtures/gh_issue_create_dependency_detect.sh` 에 미러링되고 신규 `tests/bats/skills/gh_issue_create_dependency_detect.bats` 23케이스가 트리거 매트릭스·단순 언급 negatives·cross-repo 스킵·`--no-auto-deps`·NF-1 경고를 내는 모든 id/mutation 상태·`set -e -o pipefail` 하에서의 no-match 안전성을 고정하고, 문서의 참조 정규식이 fixture 와 바이트 단위로 같은지 검사하는 drift 가드까지 포함한다 (#1424)**
