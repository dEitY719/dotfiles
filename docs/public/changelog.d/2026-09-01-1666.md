- 변경: **`gcp help scan` 의 거짓 behavior 행 2건 정정** — #913 이 제거한 "contiguous range -> bulk cherry-pick" 을 "always individual cherry-pick" 으로, #1136 이후 후보 필터일 뿐인 "same subject in base" 를 "patch-id 로 확정" 으로 교체했다. 생성 문서 `docs/guide/commands/gcp.md` 도 재생성했고, help 행과 실제 실행 경로의 일치를 bats 5건으로 고정했다.
