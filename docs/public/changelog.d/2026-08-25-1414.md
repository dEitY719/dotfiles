- 변경: **`claude/hooks/post-gh-pr-create.sh` 의 board 유무 probe(`_post_gh_pr_create_repo_has_board`)가 `_gh_project_status_normalize_repo` 를 #724 가드 뒤에서 호출한다 — 함수가 없으면 stderr 경고 한 줄을 남기고 `GH_REPO` 를 inline 으로 split 해 계속 진행한다(선행 `HOST/` 세그먼트를 떼어내므로 #1405 의 세 토막 형태도 유효하고, 슬래시가 없는 값은 정규화 함수와 동일하게 거절해 무의미한 GraphQL 왕복을 막는다). **경고는 훅이 헬퍼를 source 한 직후 foreground 에서** 출력한다 — 이를 소비하는 board 유무 probe 는 #1258 의 detach 된 tail 안에서 돌고 그 stderr 는 `/dev/null` 이라, probe 안에서 찍으면 사람에게 영영 닿지 않아 "retry-poll 은 살렸는데 배포가 어긋났다는 사실은 아무도 모르는" 상태가 된다. `350bd95d`(`/simplify`) 가 손수 짠 `${GH_REPO%/*}` 분할을 헬퍼의 정규화 함수로 교체하면서 가드 없이 `_slug=$(_gh_project_status_normalize_repo "$GH_REPO") || return 1` 로 불렀는데, 훅과 헬퍼는 각각 배포되고 워크트리 실행은 `SHELL_COMMON` 을 **메인 체크아웃**으로 해석하므로 헬퍼가 아직 그 함수를 모르는 rollout-skew 창이 실재한다 (이 저장소에서 `unknown option: --repo` 로 실측됨). 그 창에서는 `command not found`(rc 127)가 `|| return 1` 에 삼켜져 `has_board` 가 false 가 되고 **#813 retry-poll 이 경고 한 줄 없이 통째로 사라져** PR 카드가 Backlog 에 갇히는 증상이 되살아났다 — `main` 을 빨갛게 만든 `tests/bats/skills/post_gh_pr_create_hook.bats` T15·T16 이 정확히 그 회귀다. 테스트 쪽도 함께 손봤다: 7개 스텁 블록이 공용 `stub_normalize_repo` 로 그 함수를 정의해 정상 경로(함수 존재)를 검증하고, 신규 T18·T19 가 **일부러 함수 없는 옛 헬퍼**를 스테이징해 열화 경로를 고정한다(경고 출력 + retry-poll 생존 + `HOST/OWNER/REPO` 분할 정확성). 스텁만 고치면 테스트는 초록이 되지만 skew 취약점은 남으므로 둘은 짝으로만 유효하다. T20 은 그 스텁을 **실제** `gh_project_status.sh` 의 `_gh_project_status_normalize_repo` 와 7개 입력에 대해 rc·출력까지 대조하는 drift 가드다 — 손으로 쓴 스텁이 실제 계약에서 벗어나도 스위트가 초록으로 남는 구멍을 막는다(실제로 첫 버전 스텁이 `owner/` · `/repo` · `a/b/c/d` 세 건에서 이미 어긋나 있었고 이 가드가 그걸 잡아 스텁을 실제 함수와 동일하게 고쳤다). T21 은 슬래시 없는 `GH_REPO` 가 GraphQL 을 한 번도 부르지 않고 거절되는지 본다 (#1414)**
