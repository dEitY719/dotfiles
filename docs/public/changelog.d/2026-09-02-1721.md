- 변경: **전역 `git sync` alias 추가 (#1721)** — agent-toolbox 의 저장소-로컬 `scripts/git-sync.sh` 를 dotfiles 로 이식해 모든 프로젝트에서 `git sync` 로 `origin/main` + `upstream/main` 을 순서대로 병합한 뒤 `origin main` 으로 push 한다. 실행 파일은 `git/scripts/git-sync.sh`, alias SSOT 는 `git/.gitconfig` 의 `[alias] sync` — `git/setup.sh` 의 기존 `~/.gitconfig` include 모델을 그대로 타므로 setup 코드 변경은 없다. 충돌 시 `merge --abort` 없이 멈추고 안내만 하며 `.git/MERGE_HEAD` 감지로 멱등 재개한다. `upstream` 리모트가 없거나 현재 브랜치가 `main` 이 아니면 사전 가드가 안내 후 종료한다. 저장소-로컬 alias 가 git 우선순위상 전역 alias 를 덮으므로 자체 `git sync` 를 가진 프로젝트와 충돌하지 않는다. 회귀 테스트는 `tests/bats/git/test_git_sync.bats`
