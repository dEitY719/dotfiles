- 변경: **issue-watcher 동시 실행 상한을 `_IW_MAX_PER_REPO` 3→7, `_IW_MAX_CONCURRENT` 7→17 로 올리고 cron 주기를 `*/11`→`*/3` 으로 올린다 — 2026-08-28 실측(merge-train 소화 3.5분/PR vs 투입 11분/건)이 #1531 이 보류했던 "병목은 하류" 가정을 반증했다. `_IW_DISPATCH_PER_TICK` 은 1로 유지한다. 스케줄 변경은 자동 반영되지 않는다 — `aicron doctor` 가 `schedule drift` 로 보고하며 `aicron remove issue-watcher && aicron add issue-watcher` 재설치가 필요하다 (#1579)**
