- 변경: **`aicron help` 의 `status <job> --json` 를 `status <job> [--json]` 로 되돌려 같은 표의 `list [--json]` 와 선택 플래그 대괄호 표기를 통일한다 — `--json` 은 실제로 선택 인자이며, #1501 이 `ux_table_row` 의 20자 폭 문제를 라벨 줄바꿈 방식으로 근본 해결해 21자 라벨도 정렬이 깨지지 않는다 (#1499)**
