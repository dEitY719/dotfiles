- 변경: **`dEitY719/harness-skills` 레포를 신설하고 `devx-{ai-context,harness-legacy-check,harness-refactor,dissect-builtin,plugin-guide}` 5개 스킬을 `harness` 플러그인으로 이관했다 — #1410 스킬 마켓플레이스 분리 계획의 Phase 1. 스킬 디렉터리에서 `devx-` 접두사를 떼고 호출 형식은 `/devx:ai-context` → `/harness:ai-context` 로 바뀐다 (#1645)**
- 변경: **`references/{codex,kimi,gemini,antigravity,hermes,opencode}-tools.md` 6종 하네스별 툴 매핑 문서를 `harness-skills` 가 단독 소유한다 — 나머지 14개 분리 레포는 복제 대신 이 문서를 링크로 의존한다 (#1410 F-5 / NF-2). 각 문서는 툴 이름 대응표뿐 아니라 하네스별 기능 공백(`Workflow` 툴 부재, Claude Code 내장 스킬 카탈로그 접근 불가, 플러그인 캐시 의존)과 우회 절차를 함께 명시한다**
- 변경: **`harness-skills/.github/workflows/skill-check.yml` 을 `workflow_call` 재사용 워크플로로 정의해 15개 분리 레포의 CI SSOT 로 삼는다 (#1410 D-10) — `plugin-name` 입력을 받고, 기존 검사에 더해 `SKILL.md` 100줄 상한(progressive disclosure)·스킬 description 합계 5,440자 Codex 예산·`shellcheck` 3종을 새로 게이트한다**
- 변경: **`claude/plugin/marketplaces.json` 에 `harness-skills`, `claude/plugin/plugins.json` 에 `harness@harness-skills` 를 등록했다 — `claude/plugin/restore.sh` 가 새 항목을 복원 대상으로 인식한다**
- 변경: **dotfiles 의 기존 `claude/skills/devx-*` 5개 디렉터리는 그대로 남는다 — 삭제는 #1410 Phase 4 의 별도 이슈 몫이다**
