- 변경: **사내 PC 의 live `~/.claude/settings.json` 소유권을 `gateway-cli` 로 넘기고, dotfiles 는 `.hooks`/`.statusLine` 만 자동 복구하는 구조로 축소했다. 계기는 statusline 이 `-- / -- (--)` 플레이스홀더만 출력한 사고다 — 조직 LLM Gateway 전환(System LSI사업부)으로 배포된 `gateway-cli setup` 이 live settings.json 을 직접 편집하면서 `apiKeyHelper`/`awsCredentialExport`/`awsAuthRefresh`/`cleanupPeriodDays`/`env.*` 를 넣는 동시에 기존 `.statusLine.command`(`${HOME}/dotfiles/claude/statusline-command.sh`)를 자기 바이너리로 덮어썼고, 그 바이너리가 아직 플레이스홀더만 찍는 상태였다. 한 파일에 writer 가 둘이면 서로의 키를 지우는 왕복이 되므로 dotfiles 가 물러난다: **`aws/setup.sh` 의 settings.json 머지(#687)를 deprecate** 했다 — `_merge_claude_settings_json` 과 `~/.claude/settings.local.json` 자동 archive(#924 이후 그 파일은 개인 override 의 정식 슬롯이라 치우는 동작 자체가 유해했다)를 삭제하고, 실행하면 deprecation notice 만 출력한 뒤 settings.json 을 건드리지 않는다. 남은 책임은 `aws/aws.local.sh` + `~/.aws/config` 시드뿐이며(gateway-cli 는 AWS SSO 프로필·CA bundle 을 만들지 않는다) `claude/settings.bedrock-overlay.example` 도 unused 로 표시했다(삭제하지 않고 롤백 참조용 보존). 그 결과 SSOT 갱신 통로가 사라지는 공백은 **`claude/hooks/session-start-settings-drift.sh` 가 경고에서 자동 복구로 확장**되어 메운다: 비교 대상이 `.hooks` 에서 `.hooks` + `.statusLine` 로 늘고, 사내 모드(`~/.dotfiles-setup-mode` == `internal`)에서 drift 를 발견하면 그 두 키만 jq 로 live 파일에 in-place 패치한 뒤 "auto-corrected — no action needed" 로 알린다. gateway-cli 소유 키와 Claude Code 네이티브 키(`enabledPlugins`/`extraKnownMarketplaces`)는 건드리지 않고, 백업은 `~/.claude-backups/settings.json.pre-drift-heal.backup`(latest-only, #919/#806)에 남긴다. live 가 symlink 거나 SSOT 가 해당 키를 정의하지 않거나 패치가 실패하면 자동 복구를 포기하고 안내만 한다. **비사내 PC 는 동작 변화 없음** — `./setup.sh` 재시드 경로가 여전히 유효하므로 종전처럼 안내만 한다. **사내 PC 운영자 주의**: 앞으로 settings.json 재시드는 `./aws/setup.sh` 가 아니라 `gateway-cli setup` → `gateway-cli verify` 이고, 개인 설정은 live 파일 직접 편집이 아니라 `~/.claude/settings.local.json` 에 넣어야 한다(직접 편집은 gateway-cli 재실행에 지워진다)**
