- 변경: **aicron 잡 `session-doctor` 추가 (`*/1 * * * *`) — herdr 가 관리하는 모든 pane 을 매분 훑어, 마지막 assistant 턴이 정상 종료가 아니라 API 에러로 끊긴 채 `idle` 로 멈춘 탭에 `/devx:restart` 를 한 줄 주입한다.** 판정은 `herdr agent list` 의 `tab_id`/`agent_status`/`cwd` 와, 그 `cwd` 에 대응하는 최신 Claude Code transcript (`~/.claude*/projects/<slug>/*.jsonl`) 의 마지막 `assistant` 이벤트만 본다 — Claude Code 가 끊긴 턴을 남길 때 쓰는 `isApiErrorMessage`, 또는 `model` 이 `<synthetic>` 이면서 에러 문구가 함께 있을 때만 후보다. 오탐 0 이 요구사항이라, 에러 문구를 *인용한* 정상 턴·서브에이전트 실패를 전달하는 task-notification·이미 복구된 세션·transcript 가 없는 pane 은 모두 후보에서 빠진다(불확실하면 후보 제외). 주입은 `herdr agent prompt <tab_id> "/devx:restart" --wait` 로 백그라운드 실행하며, 스킬을 대신 실행하지는 않는다 — `devx:restart` 는 설계상 같은 세션 전용이고, 재개 여부는 그 세션이 스스로 정한다. 탭별 상태(`${XDG_STATE_HOME:-$HOME/.local/state}/session-doctor/state.json`)에 감지 시각·누적 주입 횟수·마지막 주입 시각을 남기고, 누적 3회(`SESSION_DOCTOR_CAP`)를 채운 탭은 더 주입하지 않고 경고만 남긴다. herdr 가 없거나 `herdr agent list` 가 실패하면 그 tick 은 조용히 건너뛰고 다음 분에 재시도하며, 주입 자체가 거부되면(예: pane busy) 경고만 남기고 재시도 예산은 소모하지 않는다. tick 중첩은 `flock` 이 막는다 (#1581)
