- 변경: **`dEitY719/devenv-skills` 레포를 신설하고 `devx-{mise-migrate,symlink-manager,ssh-delegate}` 3개 스킬을 `devenv` 플러그인으로 이관했다 — #1410 스킬 마켓플레이스 분리 계획의 Phase 1. 새 레포에서는 스킬 디렉터리 접두사가 `devx-`에서 빠져 `/devenv:mise-migrate` 형태로 호출한다 — dotfiles 의 기존 `claude/skills/devx-*` 원본은 그대로 남아 `/devx:mise-migrate` 호출도 계속 동작한다(NF-1, 삭제는 Phase 4) (#1642)**
- 변경: **새 레포는 6개 하네스 매니페스트(Claude Code / Codex / Kimi / Hermes / OpenCode / Gemini·Antigravity)를 레포 루트에 두고 `skills/` 단일 평면 트리를 가리키는 구조이며, 하네스별 툴 매핑 문서는 복제하지 않고 `harness-skills/references/` 를 링크로 참조한다 (#1410 F-5 / NF-2)**
- 변경: **CI 는 독립 workflow 대신 `harness-skills` 의 `workflow_call` 재사용 워크플로(`skill-check.yml@main`)를 `plugin-name: devenv` 로 호출한다 — 이슈 NF-2 가 예고한 "나중에 전환" 후속 작업은 `harness-skills` 가 이미 완료돼 불필요해졌다 (#1410 D-10)**
- 변경: **`ssh-delegate` 의 런타임 산출물 이름(`~/.ssh/config.d/devx-delegations`, `~/.local/state/devx/ssh-delegations.log`, `DEVX_SSH_*` 환경변수)은 의도적으로 바꾸지 않았다 — 이미 디스크에 존재하는 상태라 개명에는 마이그레이션이 따로 필요하다. 이번 이관은 호출 문자열만 `devx:` → `devenv:` 로 바꾼다**
- 변경: **`claude/plugin/marketplaces.json` 에 `devenv-skills`, `claude/plugin/plugins.json` 에 `devenv@devenv-skills` 가 등록됐다 — `claude/plugin/restore.sh` 가 새 항목을 복원 대상으로 인식한다. 두 JSON 자체는 `claude plugin install` 직후 publish-sync 크론이 8d3dc25f 로 먼저 커밋했으므로 이 PR diff 에는 나타나지 않는다 — 대신 등록을 지키는 회귀 테스트를 `tests/bats/tools/claude_plugin_scaffold.bats` 에 추가한다**
