- 변경: **PreToolUse 훅(`claude/hooks/bash_wait_loop_guard.py`)이 루프 종료 조건으로 쓰인 자기매칭 `pgrep -f` 와 `tasks/*.output` 무한 폴링 루프를 Bash 실행 전에 기계적으로 차단한다 — 판정은 등장 횟수가 아니라 루프 구조 기준이며, 일회성 `pgrep -f` 와 하네스 권장 `until … sleep …` 일반 폴링은 그대로 허용**
