- 변경: **`gcp scan` context-drift 판별기가 JSON 후행 콤마만 다른 줄을 신규 내용으로 오판하던 문제 수정 (#1688)** — 줄 텍스트 대신 `jq` 로 파싱한 구조(`[경로, 값]` 레코드)를 비교한다. 이미 HEAD 에 흡수된 커밋이 영구 표시되어 `gcp-scan-skip.conf` 수동 등록이 선형 증가하던 노이즈가 사라진다.
- 변경: **콤마를 텍스트로 제거하는 방식을 폐기하고 구조 비교로 전환 (PR #1690 리뷰 3라운드)** — 텍스트 제거는 라운드마다 해당 문자가 의미를 갖는 자리를 새로 노출했다(Python 튜플 `x = 1,`, CSV 빈 후행 필드, Markdown hard break, 잘못된 콤마를 지우는 커밋 자체, JSON 배열 순서 변경). 전부 실제 커밋이 no-op 으로 조용히 버려지는 것으로 실측됐다. 구조 비교는 배열 인덱스를 경로에 담아 순서를 보존하고 콤마·들여쓰기 노이즈는 파싱 단계에서 사라지므로 예외 처리가 필요 없다.
- 변경: **jq 미설치·JSON 파싱 실패 시 #1688 이전 정확 비교로 자동 후퇴** — 확장자 목록을 두지 않는다. Python·CSV·Markdown 은 JSON 으로 파싱되지 않아 저절로 제외되고, `.eslintrc` 처럼 확장자 없는 JSON 설정은 저절로 포함된다. #1177 무손실 계약은 유지된다.
