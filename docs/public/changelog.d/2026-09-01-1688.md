- 변경: **`gcp scan` context-drift 판별기가 JSON 후행 콤마·trailing whitespace 만 다른 줄을 신규 내용으로 오판하던 문제 수정 (#1688)** — 줄 단위 정확 일치 대신 정규화 후 비교한다(후행 공백 + 콤마 1개 제거, 선행 들여쓰기는 YAML 의미 보존을 위해 유지). 이미 HEAD 에 흡수된 커밋이 영구 표시되어 `gcp-scan-skip.conf` 수동 등록이 선형 증가하던 노이즈가 사라진다. 실제 새 내용을 가진 커밋을 no-op 으로 버리지 않는 #1177 계약은 그대로다.
