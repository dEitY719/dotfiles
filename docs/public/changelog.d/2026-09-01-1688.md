- 변경: **`gcp scan` context-drift 판별기가 JSON 후행 콤마만 다른 줄을 신규 내용으로 오판하던 문제 수정 (#1688)** — JSON 계열 파일(`.json`/`.jsonc`/`.json5`)에 한해 후행 공백과 콤마 1개를 제거한 뒤 비교한다. 이미 HEAD 에 흡수된 커밋이 영구 표시되어 `gcp-scan-skip.conf` 수동 등록이 선형 증가하던 노이즈가 사라진다.
- 변경: **정규화를 JSON 계열로 한정하고 base 는 정확 비교를 유지 (PR #1690 리뷰)** — 정규화를 모든 파일에 적용하면 Python 튜플 `x = 1,`, CSV 빈 후행 필드, Markdown hard break 처럼 해당 문자가 의미를 갖는 커밋이 no-op 으로 조용히 버려졌다. 또 `base` 까지 정규화하면 커밋의 델타 자체가 그 문자일 때(예: JSON 잘못된 후행 콤마 제거) 커밋이 사라졌다. 두 경로 모두 회귀 테스트로 고정했고, #1177 무손실 계약은 유지된다.
