- 변경: **`claude/statusline-command.sh` 에 prompt cache TTL(`⏱️`) 세그먼트를 추가했다 — `$ENABLE_PROMPT_CACHING_1H` 환경변수를 직접 읽어 `1`이면 `⏱️ 1h`, 그 외(미설정 포함)면 Claude Code 기본값인 `⏱️ 5m`을 기존 `usage_group`(📥💰📤📜) 끝에 이어붙인다. `cost_info`와 달리 `SETUP_MODE` 게이팅 없이 전 계정/PC 공통으로 항상 노출된다 (#1385)**
