- 변경: **issue-watcher / merge-train 디스패처의 정착 대기가 고정 13초 sleep 에서 준비 신호 폴링으로 바뀐다 — `herdr agent read` 로 팬 텍스트를 읽어 연속 두 프레임이 같은 비어 있지 않은 내용이면 즉시 프롬프트하므로, 정상 팬은 13초가 아니라 1~2초에 디스패치된다 (#1570)**
- 변경: **13초는 그대로 상한으로 남는다 — 준비 신호가 끝내 오지 않으면 경고를 남기고 그대로 프롬프트를 시도한다(#1560 이전 동작 보존). `herdr agent read` 실패나 빈 출력도 "아직 준비 안 됨" 으로만 취급하고 tick 을 중단시키지 않는다 (#1570)**
- 변경: **`Not logged in` 팬은 프레임이 안정적이어도 준비 완료로 보지 않는다 — `state_change_seq` 로는 구분할 수 없는 실패 상태라 팬 텍스트를 1순위 신호로 쓴다 (#1570, #1561)**
- 변경: **폴 간격을 `IW_SETTLE_POLL_SLEEP` / `PMT_SETTLE_POLL_SLEEP` (기본 1초) 로 조정할 수 있고, `0` 이면 실제 sleep 없이 폴링만 한다 (#1570)**
- 변경: **merge-train 의 세션 재사용 경로는 여전히 정착 대기를 전혀 하지 않는다 — 폴 한 번조차 돌지 않는다 (#1570)**
