- 변경: **신규 `shell-common/tools/custom/issue_watcher_cron.sh` 추가 — issue-watcher:dispatcher 의 5분 주기 headless 재호출을 herdr workspace/agent 기반 tick 으로 대체해, 감시·디스패치 진행 과정을 herdr 화면에서 실시간으로 볼 수 있게 한다. 전용 herdr workspace(label `issue-watcher`)의 고정 pane 에 claude 세션 하나만 띄워 재사용하고, `${XDG_STATE_HOME:-$HOME/.local/state}/issue-watcher/herdr-watch.json` 로 idempotent 부트스트랩한다. 매 tick — 상태 없음 → workspace/agent 부트스트랩; agent 가 idle/done → dispatcher 프롬프트 전송; working/blocked → 스킵; agent 소실 → 재부트스트랩 후 전송. 리뷰 후속으로 `set -u`(HOME/XDG_STATE_HOME 모두 미설정) unbound-variable 가드, `flock` 기반 single-instance 락(겹치는 cron tick 이중 디스패치 방지, `flock` 부재 시 경고 후 무보호로 degrade), 손상된 상태 파일이 기본 agent 이름을 조용히 지워버리던 버그 수정, `--cwd` 가 기존 상태를 무시하고 재사용될 때의 경고 로그를 추가했다. 이슈 본문의 설계 2(이슈 발견 시 워크트리별 디스패치)는 이 저장소 밖 agent-toolbox 프로젝트 반영 대상이라 out of scope (#1389)**
