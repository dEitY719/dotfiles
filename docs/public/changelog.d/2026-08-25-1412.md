- 변경: **`claude/skills/skill-create/scripts/run_eval.py` 의 트리거 검출이 스트림 전체를 본다 — 종전에는 **첫 툴 블록에서 질의 전체를 판정**했다. `content_block_start` 가 `Skill`/`Read` 가 아니면 그 자리에서 `return False`, `content_block_stop` 은 그 시점의 누적 JSON 으로 즉시 확정, `assistant` 폴백은 content 배열 첫 항목만 보고 `return`. 그래서 평가 대상과 같은 description 을 가진 스킬이 이미 설치돼 있으면(dotfiles 는 71개 스킬을 상시 노출한다) 모델이 진짜 스킬을 먼저 호출한 뒤 uuid 프로브를 호출해도 프로브는 아예 읽히지 않아 **실제 트리거가 전건 `trigger_rate 0.0`** 으로 보고됐다 — `CLAUDE_CONFIG_DIR` 격리 여부만 바꿔도 같은 스킬/같은 쿼리가 FAIL↔PASS 로 뒤집히던 원인. 판정 상태기계를 순수 함수(`TriggerDetector` / `detect_trigger`)로 분리해 모든 블록을 훑고, 부정 판정은 스트림 종료(`message_stop`/`result`)에서만 확정한다. 프로세스 종료 후 버퍼에 남은 tail 도 이제 파싱한다(종전에는 `remaining` 을 버퍼에 담고 파싱 없이 break — 첫 블록이 판정하던 시절엔 무해했지만 지금은 필수). 함께 `stderr=subprocess.DEVNULL` 을 없앴다 — 인증 만료·중첩 세션 가드·타임아웃이 "description 이 안 걸린다" 와 똑같은 `0.0` 으로 보였기 때문이다. 실행 단위 결과가 `{"triggered", "error"}` 를 싣고, 에러 난 실행은 trigger rate 분모에서 빠지며, 쓸 수 있는 실행이 하나도 없는 쿼리는 **절대 pass 로 집계되지 않는다**(종전에는 하네스가 죽어 있으면 should-not-trigger 쿼리가 전부 조용히 "통과"했다). `--verbose` 는 `[ERROR]` 상태와 캡처한 stderr 를 함께 출력하고, 결과 JSON 의 각 항목에 `usable_runs`/`errors`/`error_samples`, `summary` 에 `errors` 가 추가된다(기존 필드 `trigger_rate`/`triggers`/`runs`/`pass` 는 그대로라 `run_loop.py`·`improve_description.py`·`generate_report.py` 는 영향 없음). stderr 는 파이프가 아니라 임시 파일로 받아 stdout 만 읽는 루프에서 파이프가 차 교착되는 일이 없다. 세 번째로, 평가 대상과 같은 이름의 스킬이 환경에 보이면 `main()` 이 경고한다 — 격리 회피책을 잊었을 때 조용히 틀리지 않게 하기 위함. 이 스킬은 마켓플레이스 `skill-creator` 를 플러그인 업데이트에 견디도록 의도적으로 복사해 온 사본이라(`b13f6686`) 재동기화 경로가 없다 — 수정은 로컬 영구 반영이고, 각 패치 지점에 `LOCAL PATCH (dotfiles #1412)` 주석과 `references/local-patches.md` 레지스트리를 남겼다 (#1412)**
