- 변경: **`issue_watcher_cron.sh` tick 이 dispatch 직전에 토큰 한도 게이트를 평가한다. 한도가 소진돼도 5분마다 최대 3건씩 계속 dispatch 하던 동작이 멈춘다 — 이건 단순 낭비가 아니라 유실이었다: 디스패처의 중복 방지 기준이 `issue-<n>` 워크트리 존재 여부라, 워크트리만 만들어지고 pane 이 한도로 조용히 멈춘 이슈는 다음 사이클부터 영구히 후보에서 빠졌다. 게이트는 연속 `2`회의 실패한 dispatch(프롬프트 전달 실패 + 그 직후 agent 가 `working`/`blocked` 가 아님)를 근거로 닫히고 `30`분간 유지되며, 그 사이 tick 은 dispatcher 프롬프트를 아예 보내지 않으므로 워크트리도 만들어지지 않는다(`exit 0`, 기존 `working|blocked` skip 과 같은 취급). 만료되면 다음 tick 이 스스로 재평가하고 정상 복귀한다 — 영구 정지 경로는 없다. 이슈가 권장한 관측 대상(`iw-<n>` pane 별 상태)은 tick 에서 주소 지정이 불가능해(이슈 번호는 pane 안의 dispatcher 가 고르고 되돌려 보고하지 않으며 `herdr agent list` JSON 에는 agent 이름 필드 자체가 없다) 같은 신호를 한 단계 위 `iw-watch` 에서 본다: 한도가 소진되면 그 claude 세션도 프롬프트를 처리하지 못해 herdr 이 `agent_prompt_stalled` 를 내고 재시도까지 stall 한 뒤 dispatch 가 실패한다. 게이트 평가가 어떤 이유로든 실패하면 항상 정상 dispatch 로 fail-open 한다 — 감지 로직이 watcher 를 멈추는 원인이 되면 막으려던 문제보다 나쁘다. 상태는 기존 state 디렉터리의 별도 파일 `rate-limit.json` 에 저장되므로 `herdr-watch.json` 스키마와 기존 파일은 그대로다. 게이트 개폐는 각각 `ux_warning`/`ux_info` 한 줄로 남고 `--help` 에도 문서화된다  게이트를 닫는 근거는 herdr 이 `agent_prompt_stalled` 를 낸 dispatch 에 한정된다 — `agent_not_found`/인증/네트워크 실패는 pane 이나 연결이 끊긴 것이지 계정이 마른 게 아니라서, 이 게이트가 고칠 수도 없는 장애로 30분을 묶고 cron 로그에 "token limit" 으로 잘못 분류하게 된다. (#1436)**
