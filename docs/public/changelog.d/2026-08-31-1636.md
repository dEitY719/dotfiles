- 변경: **`review-passed` 라벨 부여 책임을 `devx:pr-review-all`에서 `gh:pr-reply`로 옮겼다 — `devx:pr-review-all` Step 3.5는 이제 `review-blocked`만 붙이고, 비블로킹 집계에서는 기존 `review-blocked`만 해제한 뒤 무라벨(=미검증)로 남긴다.**
- 변경: **`gh:pr-reply` Step 6이 모든 리뷰 코멘트에 답변한 뒤 미해결 BLOCKER가 없으면 외부 AI CLI 재호출 없이 자체 판단으로 `review-passed`를 부여한다 — "자가 인증 금지"(NF-2) 원칙을 이 경로에 한해 의도적으로 완화한 것이며, `devx:pr-review-all` 재검토 비용과 `gh:pr-merge-train` 반복 블로킹 해소를 위한 명시적 트레이드오프다(발견은 외부 AI, 해소 확인은 `gh:pr-reply`). 미해결 BLOCKER가 하나라도 있으면 여전히 절대 부여하지 않는다.**
- 변경: **`gh:pr-merge`가 머지 성공 후 그 PR의 `review-passed` 라벨을 정리 삭제한다(soft-fail — 실패해도 머지 결과와 보고에는 영향 없음).**
- 변경: **`gh:pr-reply`의 #1616 타겟 재검토 lane(`gh:pr-review --paths` 재호출)과 `references/targeted-rereview.md`를 제거하고 `references/review-passed-gate.md`로 대체했다.**
- 변경: **`gh:pr-reply` Step 6이 매 pass 마다 판정 이력을 `<!-- pr-reply-origins:<sha> -->` 원장 코멘트로 남기고, 다음 pass 는 그 이력을 병합(리뷰어 단위 supersede)해 게이트에 넘긴다 — 앞선 pass 가 DECLINE 한 BLOCKER 가 Step 2 중복 제거에 가려 사라지고 이후 pass 가 `review-passed` 를 부여하던 구멍을 막는다(PR #1637 리뷰, codex BLOCKER).**
- 변경: **`gh:pr-reply` 의 `review-passed` 게이트가 PR 에 외부 리뷰 근거(`<!-- ai-review: -->` 마커)가 있을 때만 라벨을 부여한다 — 근거 인자는 fail-closed 기본값이라 생략하면 `hold=no-external-review` 로 무라벨(=미검증)로 남는다(PR #1637 리뷰, agy BLOCKER).**
- 변경: **origin 토큰의 리뷰어 필드가 봇 로그인(`gemini-code-assist` / `sourcery-ai` / `copilot`)도 받는다 — `gh:pr-review --ai` enum 과는 별도 집합이며, 봇이 올린 BLOCKER 가 게이트에 보이지 않던 문제를 고친다.**
- 변경: **`devx:pr-review-all` 의 반대 판정 라벨 삭제가 실패를 더 이상 삼키지 않는다 — 404(라벨 없음)는 정상 경로로 조용히 지나가고, 진짜 실패일 때만 두 판정 라벨 공존 가능성을 WARN 한다(PR #1637 리뷰, codex FOLLOW-UP).**
