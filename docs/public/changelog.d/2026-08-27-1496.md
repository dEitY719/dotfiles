- 변경: **`aicron doctor` 가 매니페스트와 실제 crontab 의 스케줄 불일치(drift)를 잡아낸다.** 기존 doctor 는 orphan 블록·누락 스크립트·손상된 state·중복 블록만 검사해, 매니페스트의 `schedule` 값이 바뀌어도 재설치(`aicron remove && aicron add`) 전까지는 "manifest, crontab and state agree" 로 조용히 넘어갔다 — PR #1488 이 `merge-train` 스케줄을 `*/23` → `*/5` 로 바꾼 뒤에도 실제 crontab 은 `*/23` 그대로였는데 doctor 는 이를 문제로 보지 않은 게 그 사례다. 이제 doctor 는 매니페스트에 있는 각 잡의 설치된 스케줄을 크론탭에서 직접 읽어 비교하고, 어긋나면 `schedule drift for job <name>: manifest says "..." , crontab has "..."` 경고와 함께 재설치 명령을 안내한다 (#1496)
