- 변경: **`shell-common/functions/gh_project_status.sh` 의 #724 self-check 가 하드코딩된 3중 `if` 체인에서 4개 이름을 도는 데이터 기반 `for` 루프로 바뀌고, 경고가 **어느 함수가 없는지 이름을 찍는다** (`[gh_project_status] BUG: <fn> undefined after source — ...`). 종전 명단은 `_gh_project_status_sync` · `_gh_project_status_query_current` · `_gh_pr_closing_issue_numbers` 세 개로 #725 리뷰 시점에 고정돼 있었는데, #1405(`350bd95d`)가 `_gh_project_status_normalize_repo` 를 **cross-file 의존성**(`claude/hooks/post-gh-pr-create.sh` 가 직접 호출)이자 `_gh_project_status_resolve_owner_repo` 의 내부 의존성으로 승격시키면서 명단이 낡았다. 그 결과 이 함수 하나만 깨져도 세 entry point 는 전부 정의된 채로 남아 `command -v` 3연속 검사가 모두 통과했고, 정작 `sync`/`query_current` 는 repo 해석에 rc 2 로 실패해 호출부의 `|| true` 에 삼켜지며 board 카드가 조용히 멈췄다 — #724 가 막으려던 바로 그 "조용한 no-op" 이 카나리아를 통과한 채 재현된 것이다. self-check 주석의 전제("하나라도 없으면 헬퍼는 통째로 고장")는 참이지만 검사는 그 **역**("셋 다 있으면 정상")을 가정하고 있었고, #1405 가 그 역을 거짓으로 만들었다. 루프 안에서 조기 `return` 을 하지 않으므로 rc 는 계속 0 이고(`|| true` 호출자와의 best-effort 계약 유지), 여러 개가 빠지면 빠진 수만큼 줄이 찍힌다. sourced 파일의 top-level 루프이므로 루프 변수 `_gh_ps_selfcheck_fn` 은 마지막에 `unset` 해 사용자의 인터랙티브 셸을 오염시키지 않는다(bash/zsh/dash 에서 확인). 테스트도 함께 손봤다: 기존 "healthy source" 케이스는 메시지 문구에 묶이지 않도록 `BUG:` 만 refute 하도록 완화했고(종전 문구 `BUG: public functions undefined` 는 이 변경 이후 존재하지 않아 검사가 공회전했다), 기존 "regressed helper" 스텁은 새 루프를 미러링하며 네 이름의 경고 줄이 **모두** 나오는지 본다(첫 실패에서 루프가 멈추지 않음을 고정). 신규 3건: (1) **실제 헬퍼 파일**에서 `_gh_project_status_normalize_repo` 정의 블록만 awk 로 도려내 source 했을 때 이름이 박힌 경고가 나오는지 — 종전에는 침묵했다. 기존 스텁 방식은 구현을 **그대로 복사**하기 때문에 명단이 불완전하다는 사실을 원리적으로 탐지할 수 없어, 실제 파일을 변형하는 방식으로 그 순환을 끊었다. (2) 루프 변수 누수 가드. (3) **재발 방지 가드** — 이 헬퍼에 정의됐고 다른 `*.sh` 에서 참조되는 함수는 전부 self-check 명단에 있어야 한다고 강제한다(현재 cross-file 호출 대상은 정확히 그 네 개, 나머지 다섯은 파일 로컬). #1405 가 착지한 순간 이 가드가 빨갛게 됐을 것이다. 스코프 경계: 이 변경은 #1414/PR #1420 의 **호출부 가드를 대체하지 않는다** — self-check 는 파일이 source 될 때 그 파일 안에서 돌므로 rollout-skew(옛 헬퍼가 source 되는 상황)를 원리적으로 알 수 없다. 두 장치는 서로 다른 실패 축(같은 파일 안의 파손 vs 배포 어긋남)을 덮는 보완 관계다 (#1421)**
