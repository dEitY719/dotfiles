- 변경: **리뷰어 판정이 이제 머지를 실제로 막는다 — `gh:pr-merge-train` 에 Step 3.5 판정 게이트를 넣어 `review-blocked` PR 은 `[SKIPPED] review-blocked — reviewer verdict is blocking`, 판정 라벨이 **아예 없는** PR 은 `[SKIPPED] review not verified — no review-passed label` 로 큐에서 빠진다. 부재는 통과가 아니라 미검증이며(#1527 확정), 시간 백스톱은 두지 않는다 — 막힌 PR 은 재리뷰 1회나 사람이 라벨 하나로 즉시 푼다. 게이트는 Step 2 의 `_gh_pr_merge_train_filter_targets` 뒤에 독립적으로 놓여 새 API 호출 없이 이미 읽은 `labels` 만 소비하고, train 은 리뷰 코멘트 본문을 절대 파싱하지 않는다. 판정표·근거: `claude/skills/gh-pr-merge-train/references/review-verdict-gate.md`. (#1564)**
- 변경: **`devx:pr-review-all` 이 드디어 판정 라벨을 *발급*한다 — 새 Step 3.5 가 각 리뷰어 레인의 판정을 집계해 `review-blocked`/`review-passed` 를 PR 에 붙인다(`devx_pr_review_all_apply_label`, 반대쪽 라벨은 무조건 먼저 제거, 전 구간 soft-fail). 파서·집계기·라벨 제거는 #1562/#1563 으로 이미 있었지만 *쓰는* 쪽이 없어 게이트가 읽을 라벨 자체가 존재하지 않았다. Step 4 의 push 보다 **앞에** 실행된다 — 뒤로 가면 `/simplify` 커밋 이후의 head sha 를 읽어 모든 레인이 stale 판정이 되고 게이트가 조용히 죽는다. (#1564)**
- 변경: **`gh:pr-review` 의 리뷰 코멘트 마커에 head sha 가 붙는다: `<!-- ai-review:<ai>:<sha> -->`. 판정은 특정 커밋 1개에 대한 주장이므로, sha 없이는 두 번 전 push 때 남은 리뷰가 현재 head 의 판정으로 재사용되어 아무도 안 본 코드에 `review-passed` 가 붙을 수 있었다. sha 는 기존 `gh pr view` 통합 호출에 `headRefOid` 를 얹어 얻으므로 추가 왕복이 없다. **이 변경 이전에 게시된 코멘트는 접미어가 없어 `unknown` 으로 읽히고(=라벨 없음, 머지 안 됨), 이는 의도된 fail-closed 방향이다** — 해당 PR 은 재리뷰하면 풀린다. (#1564)**
- 변경: **`gh:label-bootstrap` 에 `pipeline|name|color|description` 형식의 세 번째 feed 가 생겼다 — `review-blocked`(`b60205`) / `review-passed`(`0e8a16`) 를 10-label SSOT 에 편입하지 않고 프로비저닝만 하며, `--prune` 에서 항상 보존한다. `_gh_pr_edit_safe_label` 은 없는 라벨을 자동 생성하지 않으므로(#326) 이 경로가 없으면 판정 라벨이 영영 안 붙고 게이트가 모든 PR 을 영구 skip 시킨다. (#1564)**
- 변경: **롤아웃 주의: 이 게이트가 켜지는 시점에 열려 있던 모든 PR 은 판정 라벨이 없어 `[SKIPPED] review not verified` 로 처리된다. 이는 "부재는 차단" 설계가 의도대로 동작하는 것이며, `/devx-pr-review-all <PR#>` 을 한 번 돌리거나 사람이 `review-passed` 를 직접 붙이면 풀린다. (#1564)**
