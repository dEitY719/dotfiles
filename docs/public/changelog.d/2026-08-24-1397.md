- 변경: **`tests/test` 의 `run_bats()` 가 bats-core 부재/`.bats` 파일 0건일 때 기존 warn-and-return-0 대신 fail-closed(return 1) 하도록 계약을 바꿨다 — 서브모듈 미초기화 클론·워크트리에서 shell 유닛 테스트 2122건이 한 건도 안 돌았는데도 `./tests/test`(및 `git/hooks/pre-push` 가 태우는 `mise run test`)가 exit 0 으로 끝나던 구멍(#754 로 CI `Test (mise)` job 제거 후 이 로컬 훅이 유일한 자동 게이트였다). 기존 동작이 필요하면 `SKIP_LOCAL_PYTEST=1` 과 대칭인 `SKIP_BATS=1` 로 명시 opt-out. 스크립트 하단 `main "$@"` 호출에 `BASH_SOURCE` 가드를 추가해 sourcing 만으로 단위 테스트가 가능해졌다(직접 실행 동작은 그대로) (#1397)**
