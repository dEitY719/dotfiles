- 변경: **`gh:pr-review` 의 `--ai` enum 에 `hermes` 를 4번째 리뷰 레인으로 추가했다 (#1377). 이제 허용값은 `codex | agy | claude | opencode | hermes` 이며, 에러 메시지도 `allowed: codex, agy, claude, opencode, hermes` 로 바뀐다. `hermes` 는 Samsung DS 사내 AI 코딩 CLI 라 `opencode` 와 동일하게 **사내 PC 전용** 게이트가 걸린다 — `~/.dotfiles-setup-mode` 가 `internal` 이 아니면 `--ai hermes is internal-PC only (~/.dotfiles-setup-mode != internal)` 로 exit 1 이고, 바이너리가 PATH 에 없으면 종전 `Required CLI 'hermes' not found in PATH` 로 soft-fail 한다. `devx:pr-review-all` 은 이 레인을 4번째 병렬 팬아웃으로 배선해 리포트 라인이 `(agy:OK codex:SKIP opencode:OK hermes:SKIP simplify:committed)` 형태가 된다 — 다른 모든 레인과 마찬가지로 soft-fail 이라 hermes 부재/비사내/비정상 종료가 나머지 리뷰를 막지 않는다. **미검증 지점 주의**: hermes 의 비대화형 리뷰 서브커맨드가 아직 확인되지 않았다(`hermes-help`/`hermes/AGENTS.md` 는 `doctor`/`config`/`--version` 만 문서화). 현재 구현은 opencode 패턴을 그대로 따른 1차 가정(`hermes exec "<지시문>" --file "$PROMPT_FILE"`)이며 코드 주석과 `references/ai-cli-invocation.md` 에 그 사실을 명시해 뒀다 — 사내 PC 에서 `hermes --help` 로 확인한 뒤 정정해야 한다. 기존 codex/agy/claude/opencode 동작·문구·테스트 결과는 변경 없음(순수 추가).**
