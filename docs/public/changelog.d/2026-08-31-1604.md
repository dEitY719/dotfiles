- 변경: **issue-watcher 의 rate-limit 게이트가 strike 로 죽인 이슈를 `rate-limit-casualties.tsv` 에 `<repo> <number> <epoch> <attempts>` 로 기록하고, backoff 가 풀려 게이트가 다시 열리는 tick 에서 동시 실행 상한(`_IW_MAX_CONCURRENT`) 검사와 `_iw_select_candidates` 보다 먼저 그 이슈들을 재투입한다 — 죽었지만 이슈가 아직 OPEN 이라 zombie 회수 대상이 아닌 casualty 자신의 pane 이 마지막 슬롯을 차지한 채 남으면, 상한 뒤에 둔 재시도는 정작 그 상한을 채운 이슈를 영원히 굶기기 때문이다. 재투입 직후에는 live-agent/worktree 캐시를 무효화하고 다시 채워, 되살아난 세션이 그 tick 의 남은 dispatch 예산에 제대로 반영되게 한다 — 살아남은 pane 이 있으면 `herdr agent prompt` 로 재프롬프트하고, pane 이 사라졌으면 평소 dispatch 경로로 재생성한다. 종전에는 게이트가 strike 만 기억하고 어떤 이슈가 희생됐는지는 버렸기 때문에, 재개할 때마다 round-robin 이 다음 이슈로 넘어가 실제 할당량 소진이 backoff(30분)보다 오래 가면 30분마다 서로 다른 이슈를 하나씩 영구히 버리며 워크트리 슬롯만 채웠고, 사람이 죽은 pane 마다 `herdr agent prompt` 를 손으로 쳐야 했다. 성공 판정은 게이트가 할당량 회복을 확인하는 것과 같은 `_IW_LIMIT_OBSERVE_SEC` 관찰 창(`_iw_limit_observe`)을 쓰고, 성공한 이슈는 파일에서 제거한다. 재시도는 기존 `_IW_MAX_ATTEMPTS`(3) 로 묶이되 할당량이 아닌 이유로 실패한 경우에만 시도 횟수를 소모한다 — 같은 pass 의 다른 casualty 가 `working` 을 유지해 할당량 회복을 증명했는데도 계속 실패하는 이슈만 카운트가 올라가고, 아무도 살아나지 못한 pass(= 아직 할당량 소진)에서는 예산을 건드리지 않는다. 5시간짜리 실제 outage 가 3번의 재개만으로 이슈를 떨궈버리는 것을 막기 위해서다. 예산이 다 떨어지면 `Giving up on <repo>#<number> after 3 attempts.` 를 남기고 목록에서 제거한다. casualty 는 관찰 창의 마지막 poll 에서 상태를 실제로 읽었고 `working` 이 아니었던 dispatch 에만 기록한다 — herdr 이 응답하지 않은 pane 은 증거가 없으므로 희생자로 간주하지 않는다 (#1604)**
