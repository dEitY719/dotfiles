- 변경: **`gh:pr` 의 pre-push lint 가드가 zsh 세션에서 shellcheck·actionlint 를 한 번도 돌리지 않은 채 조용히 통과 처리하던 버그 수정 — 변수에 담긴 glob 패턴이 zsh 기본 모드 case 분기에서 매치되지 않던 것이 원인. `setopt LOCAL_OPTIONS GLOB_SUBST` 로 zsh 에서만 고쳤고(사용자 대화형 셸 옵션은 건드리지 않음), 회귀 테스트로 두 도구 경로 모두 zsh 세션에서 고정했다 (#1328)**
