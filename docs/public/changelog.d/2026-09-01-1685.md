- 변경: **Claude 플러그인 매니페스트를 "등록 계약"과 "머신 로컬 설치 상태"로 분리** — tracked `claude/plugin/{marketplaces,plugins}.json` 은 upstream 이 소유하는 등록 계약만 담고, 이 PC 가 설치한 항목은 gitignored `claude/plugin/{marketplaces,plugins}.local.json` 오버레이에 쌓인다. `restore.sh` 와 `reconcile.sh --check` 는 둘의 union 을 읽으므로 동작은 그대로다. 한 파일에 두 소유자가 append 하던 탓에 fork/mirror 가 upstream 의 플러그인 등록 커밋마다 충돌하던 문제(#1685)를 없앤다.
- 변경: **공용 스코프의 `chore(claude-plugin): sync manifest` 자동 커밋 제거** — `plugin-sync.sh` 는 이제 untracked 오버레이만 쓰므로 플러그인 설치/제거 후에도 `git status` 가 깨끗하다. 이 자동 커밋은 별도 private 레포인 `claude/plugin/company/` 에만 남는다. tracked 계약에 있는 항목을 uninstall 하면 훅이 계약을 고치는 대신 stderr 힌트를 남긴다.
- 변경: **`git/config/gcp-scan-skip-paths.conf` 의 매니페스트 항목 4개 은퇴** — 그 항목들이 우회하던 원인(매니페스트 sync 커밋) 자체가 사라졌다. path-allowlist 메커니즘과 문서는 그대로 남는다.
