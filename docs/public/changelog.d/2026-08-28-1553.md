- 변경: **`issue_watcher_cron.sh` 의 herdr 에이전트 이름 도출 실패 WARN 문구를 정정한다 — 길이 초과로 거부된 이슈는 다음 tick 에서도 같은 이유로 반복 거부되는 영구 스킵임을 명시한다. 기존 문구("skipping.")는 일시적 실패와 구조적 불가를 구분하지 못했다. 동작(fail-closed 거부) 자체는 그대로다. (#1553)**
- 변경: **`herdr_agent_name.sh` 헤더의 길이 예산표를 정정한다 — "worst-case widths" 서술을 지우고, `+5`/`+4` 항이 5자리 이슈/PR 번호를 가정한 값일 뿐임과 repo 길이별 실제 수용 자릿수(8자→14자리, 12자→10자리, 16자→6자리)를 명시한다. 코드 동작 변경은 없다. (#1553)**
