- 변경: **`./aws/setup.sh` 가 SessionStart drift-heal 훅 '자신의 등록' 1건을 다시 심는다 — 사내 PC 자동 복구의 부트스트랩 데드락 해소. 바로 위 항목으로 dotfiles 는 `.hooks`/`.statusLine` 복구를 전적으로 `claude/hooks/session-start-settings-drift.sh` 에 위임했는데, Claude Code 는 **live 파일의 `.hooks.SessionStart` 에 등록된 훅만 호출**한다. 그래서 외부 요인(gateway-cli 재설정, 손편집)이 live `.hooks` 를 통째로 날리면 그 훅의 등록도 함께 사라지고, 훅은 호출조차 되지 않아 스스로를 되살릴 수 없다 — 자동 복구가 영구 정지하는 단일 실패점이다. 같은 날 `aws/setup.sh` 의 재시드 경로(#687)까지 삭제되면서 자동이든 수동이든 복구 수단이 하나도 남지 않았다. 이제 `aws/setup.sh` 가 사내 모드에서 `_reregister_session_start_drift_hook`(F-7b)을 실행한다: dotfiles SSOT(`claude/settings.json`)에서 훅 커맨드 문자열을 jq 로 읽어(하드코딩하지 않으므로 비표준 체크아웃 경로도 동작) live `.hooks.SessionStart` 에 그 항목이 있는지 보고, 없을 때만 그 **1건만** append 한다. **스코프는 의도적으로 최소** — #687 식 deep-merge 의 부활이 아니다: 대입하는 곳은 `.hooks` 뿐이고 그 안에서도 `.SessionStart` 뿐이라 gateway-cli 소유 키(`apiKeyHelper`/`awsCredentialExport`/`awsAuthRefresh`/`cleanupPeriodDays`/`env.*`)는 읽지도 쓰지도 않는다. live 파일이 **없으면 만들지 않고**(생성 소유자는 gateway-cli), symlink 면 건드리지 않으며(그 레이아웃은 심링크를 만든 쪽 소유), 이미 등록돼 있으면 파일을 쓰지 않는다(무출력·byte-identical). 쓰기 전 `~/.claude/settings.json.pre-sessionstart-hook-reg.backup` 로 백업하고(latest-only, #806 — 훅 자신의 `.pre-drift-heal.backup` 과 계보가 겹치지 않게 suffix 분리), mktemp→jq→mv 순서라 중간 실패가 live 파일을 반쯤 쓴 상태로 남기지 않는다. jq 미설치·SSOT 에 훅 정의 없음·JSON 파싱 실패는 경고 후 no-op 이며 AWS 시드 결과를 막지 않는다. `aws/diagnose.sh` 의 `.hooks` drift 힌트도 "새 세션 시작하면 자동 복구됨"에서 `./aws/setup.sh` 재실행을 함께 안내하도록 고쳤다 — 훅 등록 자체가 지워진 경우엔 새 세션만으로는 영원히 복구되지 않기 때문이다. **비사내 모드는 동작 변화 없음**(모드 게이트에서 즉시 종료). **운영자 주의**: 자동 복구 메시지("auto-corrected")가 아예 보이지 않고 `jq '.hooks.SessionStart' ~/.claude/settings.json` 이 비어 있으면 `./aws/setup.sh` 재실행 후 Claude Code 재시작 (#1364)**
