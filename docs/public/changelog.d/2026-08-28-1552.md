- 변경: **`issue_watcher_cron.sh` 가 watch 목록의 repo basename 충돌을 tick 시작 시 경고한다 (issue #1552).** `herdr_agent_name.sh` 의 reachability 주석은 owner 충돌이 도달 불가하다는 근거로 `docs/.ssot/watched-repos.json` 을 들었지만, 그 파일은 issue-watcher 를 게이트하지 않는다 — 실제로 읽는 목록은 untracked 인 `${IW_WATCHED_REPOS:-~/.agent-factory/avatars/issue-watcher/watched-repos.json}` 이다. 주석이 두 목록을 모두 명시하도록 고치고, `issue_watcher_cron.sh` 의 tick 이 watch 목록을 읽은 직후 basename 이 중복되면 `ux_warning` 을 내는 런타임 가드를 추가했다 — 차단하지 않는 advisory 신호다.
