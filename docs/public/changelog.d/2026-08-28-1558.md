- 변경: **`claude/hooks/plugin-sync-session.sh` 가 SessionStart/Stop 훅에서 트리거하는 벌크 재동기화 커밋 제목도 이제 실제로 추가/삭제/변경된 plugin/marketplace 키를 나열한다 — 기존엔 `chore(claude-plugin): sync manifest` 로만 찍혀 `git log --oneline` 만으론 무엇이 바뀌었는지 알 수 없었다(#1430 은 단일 install/uninstall 경로만 고쳤고 이 벌크 경로는 사각지대로 남아 있었다). `_changed_keys_marketplaces`/`_changed_keys_plugins`/`_build_sync_title` 를 `claude/plugin/reconcile.sh` 에서 `shell-common/functions/plugin_sync_title.sh` 로 추출해 `plugin-sync.sh` 와 공유하며, +/- prefix + 4개 초과 시 "외 N개" 축약 규칙은 두 경로가 동일하다. 단일 install/uninstall 커밋 제목 형식은 회귀 없다. (#1558)**
