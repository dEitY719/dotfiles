- 변경: **이슈 #1443 의 검증 체크리스트 중 `agent_pane_busy` 전용 재시도 3건을 "구현하지 않음(superseded)" 으로 확정 정정 (#1459).** #1443 은 herdr-workspace 상주 에이전트 구조를 전제로 세밀한 재시도(1초 간격 최대 10회)를 요구했으나, 검토 중 머지된 PR #1440 이 `issue_watcher_cron.sh` 를 per-issue 크로스 리포 디스패처로 재작성하면서 `_iw_process_issue()` 가 워크트리+워크스페이스+탭+에이전트+프롬프트 파이프라인 전체를 `_IW_MAX_ATTEMPTS=3` 회(매 시도 정리 후 재시작) 재시도하게 됐다. 더 거친 단위지만 "일시적으로 바쁜 pane" 복원력을 이미 제공하므로 이를 충분한 커버리지로 받아들이고, `agent_pane_busy` 전용 재시도는 두지 않는다. 코드 변경은 없다 — 결정이 PR #1449 커밋 메시지에만 남아 있어 이슈만 보면 미구현으로 읽히던 불일치를 바로잡은 것이다. 추적 소스가 다시 어긋나지 않도록 이 changelog 뿐 아니라 원본인 #1443 본문도 함께 정정했다: 해당 3건을 검증 체크리스트에서 빼 `### superseded` 블록으로 옮기고 근거를 남겼다. `devx:pr-verify-merged` 로 머지 커밋 `9ca31d8d` 의 신선한 클론을 검증하다 발견했다.
