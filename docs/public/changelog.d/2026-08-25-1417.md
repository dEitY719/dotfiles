- 변경: **`#1411` 의 description 다이어트가 트리거 정확도를 해쳤는지 실측하는 회귀 장치가 생겼고, 그 첫 실행이 실제 회귀 2건을 잡아 되돌렸다 — 표본 16종에 `claude/skills/<skill>/evals/trigger-eval.json`(각 20쿼리, should-trigger 10 / should-not 10)을 커밋했고, 수동 하네스 `claude/tools/run-trigger-eval.sh` 가 `bd91d5dc^`(다이어트 직전)의 description 과 워킹 트리의 description 을 같은 세트에 물려 `after >= before - 5%p` 계약을 판정한다. 1,920 프로브 실측 결과 14/16 PASS, 미달 2건은 **같은 경쟁 쌍**이었다: `#1411` 이 `devx:pr-verify-live` 와 `devx:pr-verify-merged` 양쪽에서 상호 경계 문장(`Sister skill of [[...]] — ... live=serving-checkout identity, merged=fresh-clone identity`)을 함께 지웠고, 그 결과 서로의 should-trigger 쿼리에 오발화했다(live 의 reject 9/10 → 5/10, 점수 85% → 65%; merged 는 10/10 → 8/10, 90% → 75%). recall 은 거의 그대로고 reject 만 무너졌다 — 축소가 "무엇을 하는가"는 남기고 "무엇이 아닌가"만 지웠다는 뜻이다. `#1411` 결정 D-1 의 예외 규칙대로 두 description 을 WARN 밴드(251~400자)로 되돌리되 되돌린 문안을 매번 재측정했고, 그 과정에서 **부정 트리거는 reject 를, 긍정 판별자("no running app" 같은 언제-쓰는가 조건)는 recall 을 고친다**는 게 분리 측정으로 드러났다 — live 는 경계만 잃어 경계 복원만으로 90% 회복(379자), merged 는 둘 다 잃어 경계만 돌려주면 오발화는 멈추지만 recall 이 5/10 으로 떨어져 둘 다 복원해야 90% 로 돌아왔다(388자). 근거는 각 SKILL.md frontmatter 의 `description:` **위** YAML 주석에 남겼다 — Check 16 추출기가 그 위치를 세지 않으므로 측정 길이를 오염시키지 않는다. 71종 총량은 16,852 → 17,131자로 `#1411` 의 18,000자 목표 이내이며 PASS 69 / WARN 2 / FAIL 0. 반대 방향 사례도 관측됐다: `gh:pr-resolve-conflict` 의 reject 는 6/10 → 10/10 으로 **올랐다**(축소가 곁가지를 걷어내 경계가 또렷해진 경우) — 다이어트 자체가 아니라 경계 문장을 지웠는지가 변수다. 하네스는 `run_eval.py` 를 수정하지 않고(그건 `#1412` 소관) 프로브 쌍둥이를 만드는 4가지 경로를 모두 봉인한다: 설치된 스킬(`CLAUDE_CONFIG_DIR` 격리), 이전 실행 잔여물(잡마다 새 프로브 프로젝트), **동시 실행 프로브**(`--num-workers 1` 고정 + 병렬화를 잡 단위 `--jobs` 로 이동 — 동일 쿼리에서 `--num-workers 3` 이 0/3 FAIL, `1` 이 3/3 PASS 로 갈리는 신규 발견), 실제 체크아웃 오염(`PYTHONPATH`). 임시 설정 디렉토리와 복사한 credentials 는 `trap ... EXIT INT TERM` 으로 반드시 삭제된다. 모델은 `--model sonnet` 로 고정한다 — `run_eval.py:135-143` 의 첫-블록 검출기가 opus-5 의 스트림 모양을 못 읽어 동일 쿼리가 opus 0/2 FAIL · sonnet 1/1 PASS 로 갈리는데, 계약이 두 description 사이의 델타라 모델은 최대화가 아니라 양쪽 arm 고정만 필요하다. 음성 대조군으로 도구 자체의 유효성도 검증했다: 키워드(`mise`/`uv`/`venv`/`pip`/`Python`/슬래시 리터럴/한국어 트리거)를 전부 지우고 의미만 패러프레이즈한 변형은 점수가 **전혀** 안 떨어졌고(7/10 유지), 완전 무관 도메인 description 은 0/10 으로 붕괴했다 — **매칭은 의미 기반이지 문자열 기반이 아니며**, 이것이 `#1411` 의 다이어트가 대체로 안전했던 메커니즘이다. 재현성 한계도 실측해 문서화했다: 동일 description·동일 세트 3회 재측정이 70% / 85% / 80% 로 흩어져 **재측정 폭(15%p)이 계약 마진(5%p)의 3배**이므로, ±5%p 안쪽 델타는 "변화 미검출"로 읽고 단일 측정으로 스킬을 판정하지 않는다. 세트의 불변식(20쿼리 10/10 분할, 경쟁 3쌍의 양방향 교차 ≥4건, 슬래시 리터럴의 `#1410` 리네임 표시, 이모지·`\uXXXX` 금지)은 `tests/integration/test_trigger_eval_sets.py` 132건이 CI 에서 고정한다. 절차 SSOT 는 `claude/skills/skill-check/references/trigger-eval-procedure.md`(Check 16 에서 링크), 실측 기록은 `docs/guide/learnings/skill-description-trigger-eval.md`. 세트는 자연어 쿼리 위주로 작성해 `#1410` 의 네임스페이스 리네임 후에도 그대로 재사용된다 (#1417)**
