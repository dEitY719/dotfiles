- 변경: **`dEitY719/notes-skills` 레포를 신설하고 `write-{rca,insight,release-note,task-history,blog-dev-learnings}` 5개 스킬을 `notes` 플러그인으로 이관했다 — #1410 스킬 마켓플레이스 분리 계획의 Phase 1. 새 레포에서는 스킬 디렉터리 접두사가 `write-`에서 빠져 `/notes:rca` 형태로도 호출 가능해진다 — dotfiles 의 기존 `claude/skills/write-*` 원본은 그대로 남아 `/write:rca` 호출도 계속 동작한다(NF-1, 삭제는 Phase 4) (#1643)**
- 변경: **`notes-skills` 의 CI 는 독립 workflow 대신 `harness-skills` 의 재사용 워크플로(#1410 D-10)를 처음부터 호출한다 — 이슈 NF-2 가 독립 workflow 를 예고했던 근거("harness-skills 동시 진행 중")가 #1645 머지로 해소됐다**
- 변경: **`harness-skills` 재사용 워크플로에 `allow-emoji-paths` 입력을 추가했다 (기본값 빈 문자열, dEitY719/harness-skills#1) — `notes:blog-dev-learnings` 는 이모지를 쓴 한국어 블로그 헤딩 작성법을 가르치는 스킬이라 `references/` 예시에 실제 글리프가 필요하다. 예외는 `skills/blog-dev-learnings/references/` 한 경로뿐이며 기존 caller(`harness`·`packaging`)의 레포 전역 이모지 금지는 그대로다**
- 변경: **`claude/plugin/marketplaces.json` 에 `notes-skills`, `claude/plugin/plugins.json` 에 `notes@notes-skills` 를 등록했다 — `claude/plugin/restore.sh` 가 새 항목을 복원 대상으로 인식한다**
