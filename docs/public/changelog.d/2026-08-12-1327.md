- 변경: **`/devx:session-close` 스킬 신설 — 세션을 닫기 전 "남은 작업 없나"를 고정된 4개 항목(C-1 git 상태 · C-2 TodoList · C-3 임시 산출물 · C-4 산출물 후속)으로 감사하고 `[OK]`/`[BLOCKED]` 판정을 내린다. `devx:restart`(재개) · `devx:session-handoff`(인계)와 함께 세션 생애주기의 종료 국면을 맡는다. 판정 기준은 하나 — **세션이 죽으면 복구 불가능한가**: 진행중 merge/rebase/cherry-pick · 미커밋 변경 · untracked 파일 · 원격 미반영 커밋 · `in_progress` todo 는 BLOCKED 이고, 디스크나 GitHub 에 남아 이어받을 수 있는 stash · scratchpad 잔재 · 열린 이슈/PR 은 NOTE 다. 대상 저장소는 cwd 하나가 아니라 이번 세션이 건드린 워크트리 전부이며(`--repos <path,...>` 로 보탤 수 있다 — 치환이 아니다), "어떤 저장소를 건드렸나"는 대화를 읽어야 아는 판단이라 SKILL.md 가 목록을 만들고 `lib/check-repos.sh`·`lib/check-artifacts.sh` 가 그 목록을 기계적으로 검사하는 판단/검사 분리 구조다. 사내PC 오탐 방지: `~/.dotfiles-setup-mode` 가 `internal` 이고 origin 호스트가 `github.com`(GHES 아님)이면 원격 미반영 커밋을 BLOCKED 가 아니라 NOTE 로 강등한다 — `docs/.ssot/pc-environment.md` §3 기준 그 조합은 pull only 라 로컬에만 있는 커밋이 정상 상태이고, 그대로 두면 사내PC 세션이 영원히 닫히지 않는다(레거시 숫자 모드값 `2` 도 같이 인식). 0 바이트 untracked 파일은 단순 나열이 아니라 "버려진 예약 파일"로 따로 분류해 제시한다 — 이름만 잡아두고 내용을 못 채운 채 턴이 끊긴 신호이기 때문이다. **read-only 전용**이라 커밋·원격 전송·파일 생성/수정/삭제를 하지 않고 조치 명령은 출력만 하며, 그린이어도 `/exit` 를 대신 실행하지 않는다(`/exit` 는 CLI 내장 명령이라 스킬에서 부를 경로가 없고, 프로세스 강제종료는 transcript 정리를 건너뛴다) — 마지막 두 줄 `[OK] … / Next: /exit` 가 UX 전부다. 네트워크를 쓰는 곳은 C-4 뿐이고 실패해도 경고 1줄로 낮춰 판정을 뒤집지 않는다. 검사 대상이 0개면 조용히 통과하지 않고 "검사 대상 없음"을 명시한다. 알려진 공백: 실행중인 백그라운드 작업·서브에이전트는 스킬 안에서 열거할 확인된 표준 수단이 없어 경고 1줄로 건너뛰며, `references/checks.md` 에 Open Question 으로 기록했다 (#1327)**
