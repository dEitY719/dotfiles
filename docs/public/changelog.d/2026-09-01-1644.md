- 변경: **`dEitY719/pkm-skills` 레포를 신설하고 `{obsidian-session-clip,obsidian-resolve-conflict,karakeep-add,karakeep-classify}` 4개 스킬을 `pkm` 플러그인으로 이관했다 — #1410 스킬 마켓플레이스 분리 계획의 Phase 1. 개인 Obsidian vault 와 Karakeep 북마크처럼 *사용자 개인 데이터* 에 쓰는 스킬만 모은 축이며, 작업 대상 repo 의 `docs/` 에 쓰는 `notes-skills` 와는 출력 대상이 달라 별도 레포로 분리했다 (#1644)**
- 변경: **새 레포에서는 스킬 디렉터리 접두사(`obsidian-`, `karakeep-`)를 유지한다 — 한 플러그인 안에서 서로 다른 외부 서비스 두 개를 구분해야 하고, `/pkm:karakeep-add` 는 #1410 F-4 가 막으려는 접두 중복(`pkm:pkm-...`)이 애초에 생기지 않는다. 호출 형태는 `/pkm:obsidian-session-clip` 처럼 바뀌고, 기존 dash-form alias(`/karakeep-add` 등)는 새 레포에서 제거됐다**
- 변경: **dotfiles 의 기존 `claude/skills/{obsidian-*,karakeep-*}` 4개 디렉터리는 그대로 남아 `/obsidian:session-clip`·`/karakeep:add` 호출도 계속 동작한다 — 삭제는 #1410 Phase 4 의 별도 이슈 몫이다 (NF-1)**
- 변경: **`pkm-skills` 는 공용 자산을 소유하지 않는다 — 하네스별 툴 매핑(`references/*-tools.md`)은 `harness-skills` 링크로 의존하고, CI 는 독립 workflow 대신 `harness-skills` 의 `workflow_call` 재사용 워크플로를 `plugin-name: pkm` 으로 호출한다 (#1410 F-5 / D-10). 이슈 NF-2 는 `harness-skills` 동시 진행을 이유로 독립 workflow 시작을 지시했으나, #1645 로 이미 완료돼 전환 후속 작업을 만들 이유가 사라져 처음부터 재사용 워크플로를 쓴다**
- 변경: **`claude/plugin/marketplaces.json` 에 `pkm-skills`, `claude/plugin/plugins.json` 에 `pkm@pkm-skills` 를 등록했다 — `claude/plugin/restore.sh` 가 새 항목을 복원 대상으로 인식한다**
