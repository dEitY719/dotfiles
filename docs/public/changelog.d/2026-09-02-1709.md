- 변경: **merge-train cron 디스패처가 큐 지문 `(PR번호, headRefOid, mergeStateStatus, mergeable, verdict 라벨)` 을 틱마다 저장·비교해, 직전 틱과 동일하면 세션을 깨우지 않고 건너뛴다(backoff 창 1 → 2 → 4 → 8틱 상한, 지문 변화 시 즉시 1로 초기화). 지문은 이미 하던 `gh pr list` 응답에서 필드 3개를 더 받아 만들며 추가 호출은 없고, 건너뛴 틱은 `Queue unchanged ...` 한 줄로 남아 대상 0건인 `No target PR` 조용한 종료와 구분된다. 상태 파일은 대상별로 하나씩 `${XDG_STATE_HOME:-$HOME/.local/state}/pr-merge-train/backoff-<host>-<owner>-<repo>` 이며(틱 락은 종전대로 머신당 1개), 읽히되 쓰이지 않는 상태 파일은 건너뛰지 않고 그대로 틱을 수행한다 (#1709)**
