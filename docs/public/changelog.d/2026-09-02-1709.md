- 변경: **merge-train cron 디스패처가 큐 지문 `(PR번호, headRefOid, mergeStateStatus, verdict 라벨)` 을 틱마다 저장·비교해, 직전 틱과 동일하면 세션을 깨우지 않고 건너뛴다(backoff 창 1 → 2 → 4 → 8틱 상한, 지문 변화 시 즉시 1로 초기화). 지문은 이미 하던 `gh pr list` 응답에서 필드 2개를 더 받아 만들며 추가 호출은 없고, 건너뛴 틱은 `Queue unchanged ...` 한 줄로 남아 대상 0건인 `No target PR` 조용한 종료와 구분된다. 상태 파일은 `${XDG_STATE_HOME:-$HOME/.local/state}/pr-merge-train/backoff` (#1709)**
