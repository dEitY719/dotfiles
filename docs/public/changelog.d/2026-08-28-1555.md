- 변경: **watched-repos 레지스트리를 하나로 단일화했다** — `docs/.ssot/` 아래 있던 추적 파일(`gh:pr-post-merge-verify` 전용)을 삭제하고, `issue_watcher_cron.sh` 가 이미 읽던 untracked 배열 파일(`${IW_WATCHED_REPOS:-${HOME}/.agent-factory/avatars/issue-watcher/watched-repos.json}`)로 통합했다. `verify_skill` 필드가 각 배열 항목에 흡수됐고 옛 `main_checkout` 필드는 기존 `path` 필드로 대체됐다. **운영자 조치 필요**: 옛 추적 파일에 등록됐던 `verify_skill`(`dEitY719/dotfiles` → `devx:pr-verify-merged`)을 untracked 파일의 해당 항목에 수동으로 옮겨 적어야 `gh:pr-post-merge-verify` 가 계속 동작한다 — 옮기기 전까지는 조용히 no-op 되며, 무인 머지 자체는 영향받지 않는다.
