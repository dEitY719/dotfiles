- 변경: **`pytest tests/integration/` 가 수집 단계에서 에러로 죽던 결함을 고쳤다 — `tests/AGENTS.md` 가 문서화한 그 명령은 `exit 1` 로 실패했고, 직렬(`-n 0`)로 돌리면 수집이 Interrupted 되어 통합 테스트 1350건이 단 하나도 실행되지 않았다. 원인은 최상위 모듈 이름 `scripts` 의 충돌이다: repo-root `scripts/` 는 `__init__.py` 가 없는 **네임스페이스** 패키지인데 `claude/skills/skill-create/scripts/` 에는 빈 `__init__.py` 가 있어 **정규** 패키지였고, 정규 패키지가 이름을 독점한다. `tests/integration/test_skill_create_run_eval.py` 는 `run_eval()` 의 ProcessPoolExecutor 가 `run_single_query` 를 정규화된 이름으로 피클하기 때문에 스킬 디렉터리를 세션 내내 `sys.path` 에 올려둬야 하고(#1422), 그래서 `#1429` 가 들여온 `scripts.maintenance.check_codex_skills_budget` import 가 수집 시점에 `ModuleNotFoundError` 로 죽으면서 스위트 전체가 Interrupted 되었다. 수정은 그 빈 `__init__.py` 를 지우는 것이다 — 두 디렉터리가 모두 네임스페이스 *portion* 이 되어 `scripts.__path__` 가 양쪽을 아우르고 `scripts.run_eval` 과 `scripts.maintenance` 가 나란히 공존한다. 이슈가 적어 둔 F-1(`spec_from_file_location` 으로 고유 이름 로드)은 그대로는 성립하지 않는다: `run_eval.py` 자신이 `from scripts.utils import ...` 를 하므로 어떤 이름으로 로드하든 `scripts` 가 스킬 디렉터리로 묶여 있어야 하고, 충돌은 그대로 남는다. `sys.modules.pop("scripts")` 도 함께 제거했다 — `_NamespacePath` 는 `sys.path` 변경 시 스스로 재계산하므로 불필요하다. **정정 (#1448)** — 이 항목은 최초 게시 시 기본 실행 경로(`mise run test` · `./tests/test` · `pytest tests/integration`)가 초록이라 결함을 가렸다고 적었으나, `devx:pr-verify-merged` 가 PR #1435 의 머지 커밋 `cd51e8ae` 를 검증하다 실측으로 반증했다(같은 서술을 담고 있던 PR #1435 본문도 함께 정정했다 — 그 PR 본문의 `## 정정 (#1448)` 절에서 확인할 수 있다). 수정 전 커밋 `25f47185` 를 신선한 클론(`--no-hardlinks`, `env -u PYTEST_ADDOPTS`)에서 문서화된 명령 그대로 돌리면 `1350 passed, 1 error`, `EXIT=1` 로 **빨갛다**(대조군: 그 머지 커밋은 같은 클론에서 `1517 passed`, `EXIT=0`). `--collect-only` 도 `-n auto` · `-n 0` 양쪽 모두 `Interrupted: 1 error during collection` 으로 비영(非零) 종료한다. xdist(`-n auto`)가 바꾸는 것은 결함의 가시성이 아니라 **실패의 모양**이다: 워커가 각자 프로세스에서 수집하므로 `sys.path` 오염이 전파되지 않아 1350건은 실행·통과하고, 컨트롤러의 수집 에러 1건만 남아 화면에는 "모듈 하나가 깨진 빨강" 으로 읽힌다. 스위트 전체 미실행이라는 **규모**는 직렬 경로(`-n 0`)에서만 정직하게 드러난다. 따라서 `#1397` 이 fail-closed 로 만든 `./tests/test` 경로도 이 결함을 놓치지 않았다: 빨강으로 잡았고, 다만 그 빨강이 1350건 미실행급 사건으로 읽히지 않았을 뿐이다. 신규 `tests/integration/test_collection_integrity.py` 가 걷어내는 것은 이 규모 은폐다: 서브프로세스에서 `-n 0` 직렬 `--collect-only` 를 돌려 수집 에러가 없음을 확인하는 행위 가드와, 문제의 `__init__.py` 부재를 못 박아 실패 메시지가 원인을 스스로 설명하게 하는 메커니즘 가드 두 개다. `python -m scripts.run_loop` 등 스킬의 문서화된 진입점은 영향받지 않는다 (#1432)**
