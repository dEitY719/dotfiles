- 변경: **herdr 세션을 띄우는 세 곳(`pr_merge_train_cron.sh`, `issue_watcher_cron.sh`, `gh:pr-post-merge-verify`)이 만들던 agent 이름이 전부 무효였고, 그 결과 무인 파이프라인이 단 한 번도 세션을 띄우지 못했다. 세 곳이 같은 결함의 slug 헬퍼를 복제하고 있었다 — `tr -c 'A-Za-z0-9._-' '-'` 는 집합 *바깥* 문자만 치환하므로 집합에 들어 있는 대문자와 점은 그대로 통과하는데, herdr 의 규칙은 `^[a-z][a-z0-9_-]{0,31}$` 이라 둘 다 위반이다. 이 저장소(소유자 `dEitY719`)에서는 생성되는 이름이 항상 거부됐다: merge-train 56회 시도 0회 성공, issue-watcher 21회 시도 0회 성공, post-merge-verify 는 `pmv-github.com-dEitY719-dotfiles-<N>` 가 37자라 길이까지 초과해 머지마다 실패했다. `#1512` 가 stderr 를 보존하기 전까지 76회 실패가 전부 원인 불명이었다. 이제 정규화가 `shell-common/functions/herdr_agent_name.sh` 한 곳에 있고(소문자화 → 허용 문자 외 `-` 치환 → 연속 `-` 축약 → 앞뒤 `-` 제거 → 16자 절단 → 절단이 노출한 꼬리 `-` 제거) 세 곳이 그 하나를 쓴다 — 스킬 문서 `dispatch.sh.md` 의 인라인 블록도 복제 대신 이 파일을 source 한다. 이름은 `mt-<repo>` / `iw-<repo>-issue-<N>` / `mv-<repo>-pr-<N>` 이고 최악의 경우(16자 저장소 + 5자리 번호)에도 31자로 예산 안에 든다. merge-train 에만 번호가 없는 것은 그 이름 자체가 NF-1 싱글턴 잠금의 질의 키이기 때문이다 — 번호가 붙으면 tick 마다 이름이 달라져 기존 트레인을 못 찾고 매 tick 새 트레인이 같은 base 로 동시에 머지한다. 호스트를 이름에서 뺀 것은 절충이다: `owner/repo` 가 서버 간 유일하지 않다는 #1403/#1407 의 근거는 유효하지만 32자 예산에 host·owner·repo·번호가 함께 들어가지 않으며, 실제로 그 시도가 37자짜리 거부를 낳았다. 두 호스트가 같은 저장소명을 쓰면 충돌하지만 현재 감시 대상은 단일 호스트 1개 저장소뿐이고, 두 번째 호스트가 추가되는 시점에 `<host>/<owner>` 의 짧은 다이제스트를 접미사로 붙이는 것이 정해진 후속 조치다(헬퍼 주석에 근거와 만료 조건을 남겼다). merge-train 의 **워크스페이스 라벨**은 `mt-<host>-<owner>-<repo>` 로 그대로 둔다 — herdr 은 라벨에 이 규칙을 적용하지 않고 길이 예산도 없으며, 줄이면 이미 열려 있는 워크스페이스를 못 찾게 되기 때문이다. 저장소명이 정규화 후 빈 문자열이 되면 이름을 만들지 않고 실패한다: 빈 slug 는 `iw--issue-11` 같은 herdr 이 *받아들이는* 잘못된 이름을 만들어, 거부되는 이름보다 나쁘다. bats 21건 추가(`herdr_agent_name.bats` 15 + 세 파이프라인의 이름 규칙 검증). (#1530)**
