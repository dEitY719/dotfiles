- 변경: **`shell-common/env/proxy.local.example` 의 `http_proxy`/`https_proxy` 값 끝 trailing slash 를 제거했다 — `gateway-cli verify` 가 정규화된(slash 없는) URL 을 기대하는데 템플릿 값 끝에 `/` 가 남아 있어 매 실행마다 `[!] proxy:HTTP_PROXY`/`HTTPS_PROXY` false-warn 이 났다(실제 프록시 동작에는 영향 없음). 같은 canonical format 불일치가 `shell-common/functions/devops_help.sh`(`check-proxy` 도움말)와 `docs/guide/commands/proxy.md` 예시에도 있어 함께 수정했고, 재발 방지를 위해 템플릿에 trailing slash 를 다시 붙이지 말라는 주석을 추가했다. 개인 활성 파일 `shell-common/env/proxy.local.sh` 는 gitignore 대상이라 이 변경으로 자동 반영되지 않는다 — 이미 만들어 쓰는 사용자는 본인 파일도 동일한 값으로 수동 동기화해야 한다 (#1358)**
