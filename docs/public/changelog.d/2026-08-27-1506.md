- 변경: **`gh:pr-review` 의 opencode/hermes 레인이 `_gh_pr_review_run_ai` 내부 `timeout`(기본 540초, `GH_PR_REVIEW_SLOW_CLI_TIMEOUT_SEC` 로 조정 가능)으로 실행 시간을 하드 바운드한다 — GNU coreutils `timeout` 이 있는 환경(WSL/Linux) 한정이며, 이 바이너리가 없는 stock macOS 에서는 기존과 동일하게 무제한 실행으로 degrade 한다.** 이전엔 두 레인 모두 실행 시간 제한이 코드로 없어서, 호출부(Bash 도구 호출)의 앰비언트 타임아웃이 프로세스 트리를 통째로 죽이면 잘린 stdout 조각이 완료된 리뷰로 오인돼 PR 에 게시될 위험이 있었다. 이제 내부 타임아웃이 먼저 exit 124 로 죽여 기존 `_rc -ne 0` 게이트가 코멘트 게시를 자동으로 건너뛰고, `gh:pr-review` SKILL.md Step 5 는 두 레인의 Bash 도구 호출에 ≥600000ms 타임아웃을 명시하도록 요구한다 (#1506, PR #1542 리뷰 codex 반영)
