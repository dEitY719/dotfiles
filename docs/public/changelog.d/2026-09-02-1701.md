- 변경: **`claude/setup.sh`가 접두사 없는 `~/.claude/settings.json`도 실파일로 전환 (#1701)** — 다중 계정(external/public) 모드의 `_claude_ensure_settings_copy` 호출이 지금까지 `~/.claude-<account>` 계정 디렉터리에만 적용됐고, `claude-yolo` 래퍼 없이 맨몸 `claude`를 실행할 때 쓰이는 접두사 없는 `~/.claude`는 대상 밖이었다. 그 경로가 여전히 dotfiles SSOT로 가는 write-through 심볼릭 링크로 남아 있어 설정 변경이 git 추적 파일로 새어들었다(#924/#940 재발). "심볼릭 링크 확인" verify 단계도 이 경로를 검증하도록 확장.
