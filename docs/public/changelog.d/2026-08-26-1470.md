- 변경: **병렬로 쌓인 본인 PR 을 순차로 정리·머지하는 `gh:pr-merge-train` 스킬과 cron 디스패처 `shell-common/tools/custom/pr_merge_train_cron.sh` 를 추가한다 (#1470).** `gh:issue-flow` 를 병렬로 돌리면 PR 이 여러 건 동시에 생기고, 그중 하나를 머지하는 순간 나머지가 전부 `BEHIND` 로 밀린다. 거기에 충돌과 CI 실패가 겹치면 사람이 PR 마다 상태를 보고 `gh:pr-resolve-outdated` / `-conflict` / `-ci-fail` 중 무엇을 돌릴지 고른 뒤 `gh:pr-merge` 를 부르는 일을, 머지가 진행될수록 반복해야 했다. 11단계 중 LLM 판단이 실제로 필요한 곳은 **충돌 해결과 CI 수정 두 곳뿐**이므로, 상태 판정과 스킬 라우팅은 결정론적 표(D-1)로 고정하고 스킬이 루프를 돌린다. 처리 순서는 `CLEAN` → `BEHIND` → `UNSTABLE` → `DIRTY` 인데(D-2), 직관과 반대로 보이지만 근거가 있다 — **모든 머지가 나머지 PR 을 무효화하므로 비싼 작업은 최대한 늦게 해야 한다.** `DIRTY` 를 먼저 풀면 이후 머지마다 다시 충돌할 수 있고, 마지막에 풀면 최종 base 에 대해 한 번만 풀면 된다. 같은 이유로 정리는 미리 몰아서 하지 않고 **머지 직전에 한 건씩** 하며(D-3), 그래서 각 PR 처리 직전의 상태 재조회가 선택이 아니라 필수다. 한 PR 이 막혀도 그 PR 만 건너뛰고 train 은 계속하며(F-6), PR 당 조치 시도는 3회로 제한한다(F-5). 승인 검사는 저장소 ruleset 의 `required_approving_review_count` 를 읽어 따르되, **조회에 실패하면 승인이 필요한 것으로 간주한다** — 머지는 되돌리기 어렵고 건너뛰기는 다음 tick 에 재시도되므로 두 오류의 대가가 대칭이 아니다. `gh:pr-merge-emergency` 는 호출하지 않는다(NF-2): 예외용 감사 추적 도구를 정상 경로에 쓰면 매 실행마다 incident 이슈만 쌓인다. 무인화가 새로 요구한 조건이 하나 있는데, `gh:issue-flow` Step 2.4 가 `--defer-reply 4` 로 예약한 `gh:pr-reply` 가 끝나기 전에 머지되면 리뷰 답변과 `/simplify` 수정이 통째로 날아간다 — 그래서 마지막 업데이트 후 11분이 지나지 않은 PR 은 대상에서 제외한다(D-6). cron 쪽은 얇은 디스패처로만 두었다. issue-watcher 와 달리 **train 본체는 셸이 아니라 claude 세션 안에서 도는데**, issue-watcher 의 dispatch 는 서로 독립·병렬이라 fire-and-forget 이 성립하지만 merge train 은 직렬이고 각 단계가 앞 단계의 완료에 의존하기 때문이다(D-8). 디스패처의 책임은 중복 기동 방지·대상 PR 존재 확인·세션 기동 세 가지뿐이며, 중복 기동 방지는 두 겹이다 — flock 은 tick 끼리만 막을 수 있고(tick 은 초 단위, train 세션은 분 단위라 lock 은 이미 풀린 뒤다), 그래서 결정론적 agent 이름으로 herdr 에 직접 생존 여부를 묻는 층이 따로 필요하다(NF-1). 기존 원자 스킬 5종은 변경 없이 호출만 한다. bats 33건 추가**
