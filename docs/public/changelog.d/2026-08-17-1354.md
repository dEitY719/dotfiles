- 변경: **board approval gate 가 쿼리 실패 시 fail-closed 로 바뀐다 — `_gh_project_status_query_current` 에 3분기 종료 코드 계약을 도입했다: rc 0 + 값(Status 있음), rc 0 + 빈 출력(projectV2 미부착 = 정당한 skip), rc 1 + 빈 출력(owner/repo 해석 실패 또는 `gh api graphql` 실패). 종전에는 모든 실패가 `return 0` + 빈 출력으로 뭉개져, `project` 토큰 스코프 누락·GraphQL/네트워크 오류 같은 *쿼리 실패* 가 "보드 없음" 과 구분되지 않았고 `gh:pr-merge` Step 2-B 승인 게이트가 평가조차 되지 않은 채 통과했다(PR #1352 로 `Approved` 컬럼이 사람의 명시적 `/gh-pr-approve` 로만 설정되면서 이 신호의 가치가 커진 만큼 위험도 커졌다). 이제 Step 2-B 는 rc != 0 이면 "board Status is ..." 와는 구분되는 별도 메시지로 머지를 거부한다(exit 2). 기존 escape hatch `GH_PR_MERGE_SKIP_BOARD_CHECK=1` 은 종전대로 쿼리 실행 전에 우선 적용된다. **운영자 주의** — `gh` 토큰에 `project` 스코프가 없는 저장소(보드 미사용 포함)는 지금까지 조용히 통과하던 머지가 이제 쿼리 실패로 막힐 수 있다; `project` 스코프를 부여하거나 `GH_PR_MERGE_SKIP_BOARD_CHECK=1` 을 사용한다 (#1354)**
