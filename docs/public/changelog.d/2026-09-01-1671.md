- 변경: **분리 레포 원격 계약·드리프트 감사기 추가 (#1671)** — `scripts/maintenance/check_split_skill_repos.py` 가 `claude/plugin/{marketplaces,plugins}.json` 에서 대상 레포를 유도해(손으로 유지하는 표 없음) 원격 `.claude-plugin/marketplace.json` 의 plugin 이름이 등록된 `<plugin>@<marketplace>` 와 맞는지, 분리해 간 스킬이 `claude/skills/` 원본과 내용상 드리프트했는지 검사한다. 비교 단위는 파일이 아니라 SKILL.md + `references/` 를 합친 정규화 라인 집합이라, 100줄 상한 때문에 쪼갠 구조 차이와 네임스페이스 재작성은 오탐이 되지 않는다.
- 변경: **`split-skill-repo-audit` 워크플로 + `mise run audit-split-repos` 추가** — 네트워크가 필요한 검증은 주 1회 스케줄과 수동 호출로만 돌고, `mise run test` 는 오프라인으로 남는다. 감사기의 순수 로직은 `tests/integration/test_split_skill_repos.py` 가 픽스처로 오프라인 검증한다.
