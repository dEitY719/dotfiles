- 변경: **board approval gate 의 쿼리 실패 메시지가 `project` 스코프 누락을 따로 지목한다 — `_gh_project_status_query_current` 의 종료 코드 계약을 4분기로 확장해 rc 2(= `gh` 토큰에 `project`(read:project) 스코프가 없어 실패)를 rc 1(owner/repo 해석 실패·GraphQL/네트워크 등 그 밖의 실패)에서 분리했다. `gh api graphql` 의 stderr 를 함께 캡처해 `insufficient scopes` / `Resource not accessible` 문구가 있으면 rc 2 로 분류한다. #1354 로 fail-closed 가 도입된 뒤, 스코프가 없는 토큰은 보드 부착 여부와 무관하게 `projectItems` 조회가 항상 실패하는데도 일반적인 "query failed" 메시지만 나와 운영자가 원인을 auth/스코프/네트워크 중에서 추측해야 했다 (PR #1355 codex 리뷰 BLOCKER). 이제 `gh:pr-merge` Step 2-B 는 이 경우 "could not verify board approval status (missing 'project' scope)" 와 함께 `project`(read:project) 스코프 부여를 안내한다. **정책은 그대로** — rc 2 도 종전 rc 1 과 동일하게 fail-closed(exit 2) 이며, `GH_PR_MERGE_SKIP_BOARD_CHECK=1` escape hatch 도 그대로 우선한다. 메시지 명확화이지 게이트 완화가 아니다 (#1356)**
