# Changelog

사용자 관점의 의미 있는 변경 기록. 포맷: `## YYYY-MM-DD` 헤더 아래 `- 변경: **요약**`.

## 2026-08-12
- 변경: **`/obsidian:resolve-conflict` 스킬 신설 — Obsidian vault 클론의 `git pull` 충돌을 진단 → 분류 → 해결 → 커밋 → push → peer 클론 동기화까지 한 번에 처리한다. 대상은 같은 원격을 보는 `[windows|wsl]` 두 클론이고, `~/.dotfiles-setup-mode` 를 읽어 사내PC(`internal`, GHES)와 개인PC(`external`/`public`, github.com)를 같은 명령으로 다룬다. 자동 해결 대상은 **로컬 상태 파일뿐**이다 — obsidian-git 이 만드는 `conflict-files-obsidian-git.md` 는 삭제하고, 지금 `.gitignore` 가 추적 제외로 선언한 `.obsidian/**`·`.trash/**` 는 `git rm --cached` 로 인덱스에서만 빼 디스크 파일을 남긴다. 경계 기준이 "`.obsidian/` 아래냐"가 아니라 "**지금 `.gitignore` 가 제외 중이냐**"인 이유는, 제외 선언이 곧 "이건 PC마다 다르니 추적하지 말자"는 이미 내려진 결정이기 때문이다 — 반대로 동기화가 목적인 `appearance.json` 은 자동 처리 대상에서 빠진다. 노트 본문(`*.md`)은 절대 자동 병합하지 않고 파일별 3택(로컬 유지 / 원격 채택 / 직접 편집)을 사람에게 묻는다. vault 는 obsidian-git 자동 백업 커밋 덩어리라 히스토리 재작성은 커밋마다 같은 충돌을 반복시키므로 **merge 전용**이며 강제 push 도 하지 않는다(플러그인 설정도 `syncMethod: merge`). 경로는 하드코딩하지 않고 런타임 탐지한다 — Windows 는 `/mnt/c/Users/*/Documents/ObsidianVault-PARA` glob(정확히 1개 매칭일 때만 채택, 0개면 `cmd.exe` 로 Windows 계정명 조회), WSL 은 `obsidian-para-company`/`obsidian-para` 중 존재하는 쪽(둘 다면 모드가 결정). peer 클론은 경로 표가 아니라 `git remote get-url origin` 일치로 판정하고, clean + fast-forward 가능할 때만 당긴다. `internal` 모드 PC 에서 `github.com` 원격으로의 push 는 **거부**된다(`docs/.ssot/pc-environment.md` §3 "pull only" 를 코드로 강제 — 기본 push 동작보다 우선). 옵션: `[windows|wsl]` · `--no-push` · `--no-sync-peer` · `--dry-run` · `--vault <path>` · `-h`/`--help`/`help` (#1326)**

## 2026-08-11
- 변경: **`/obsidian:session-clip` 스킬 신설 — 이번 AI 세션의 작업을 PARA vault(`obsidian-para`)의 `99-Inbox/ai-session/` 에 md 노트 1개로 클립한다. Web Clipper 가 "웹페이지 → Inbox" 인 것처럼 이 스킬은 "세션 → Inbox" 이고, 이후 PARA 분류·`00-wiki` 융합은 vault 의 `/ingest` 가 맡는다(대신 수행하지 않는다 — 사람 개입 게이트가 vault 설계의 핵심이다). 출력 경로는 `${OBSIDIAN_VAULT_DIR:-$HOME/para/project/obsidian-para}` 기준이며 `--vault` 로 덮어쓸 수 있다(`shell-common/functions/obsidian_claude.sh` 의 TilNote vault 와는 별개 저장소다). 세션 중 커밋 1개 이상 또는 PR 생성이면 `session_type: code`(요약/변경사항/설계 근거/검증/관련/다음 단계), 아니면 `research`(질문/조사한 것/결론/근거/미해결)로 자동 분기한다. `## 메모` 3개 하위 섹션은 AI 가 모두 채우되 frontmatter 에 `memo: ai-generated` 를 남겨 `/ingest` 가 "승인"이 아니라 "교정"을 요구하도록 한다 — 사람이 백지에서 시작하지 않으면서도 게이트는 유지되는 절충이다. 옵션: `[description]` · `--no-commit` · `--dry-run` · `--vault <path>` · `-h`/`--help`/`help` (#1321)**
- 변경: **`obsidian:session-clip` 의 병렬 세션 안전장치 — 사용자는 worktree 별로 N개 세션을 동시에 돌리고 vault 는 obsidian-git 의 자동 백업 루프와도 공유되므로, 커밋은 `git add -- <파일 1개>` + `git commit -- <그 경로>` 로 pathspec 을 한정한다(`-a`/`-A`/`git add .` 금지 — 다른 세션의 반쯤 쓰인 노트를 삼킨다). `.git/index.lock` 충돌은 지수 백오프로 5회 재시도하고, 끝내 안 풀리면 노트를 디스크에 남긴 채 경고 후 exit 0 한다(obsidian-git 이 다음 주기에 회수). add~commit 사이에 obsidian-git 이 선점하면 `git diff --cached --quiet` 로 감지해 "이미 반영됨"으로 정상 처리한다. 원격 push 는 하지 않는다 — N개 세션이 동시에 push 하면 non-fast-forward 로 깨진다. 파일명은 `YYYY-MM-DD-HHmm-<repo>-<slug>.md` 로 시각과 repo 를 함께 넣어 충돌을 회피하고, 그래도 겹치면 `-2`…`-10` 으로 증가한다. vault 원본이 Windows 파일시스템에 있으므로 `\ / : * ? " < > |` 와 제어문자를 제거하고 100자로 자르되, 남는 이름이 없으면 조용히 넘어가지 않고 non-zero 로 죽는다(`obsidian-para` PR #5 의 `safe_name` 무음 실패와 같은 함정). 산출물은 `lib/verify-clip.sh` 가 파일명 금지문자·frontmatter 9키·`status: unprocessed`·메모 3섹션 비어있음을 매 실행 끝에 자체검증한다 (#1321)**
- 변경: **`gh:pr` 이 upstream 이 *다른 이름의* 브랜치를 가리키는 상태(`@{u}` ≠ `origin/<현재 브랜치>`)를 push 정책 표의 독립 행으로 인식 — 이제 항상 `git push -u origin HEAD` 로 재페어링한다. 종전 3행 표에는 이 상태에 맞는 행이 없어 2행("upstream 존재 · ahead · divergence 없음")으로 떨어졌고, 그 처방인 맨 `git push` 는 `push.default=upstream` 에서 피처 브랜치의 커밋을 upstream 브랜치(보통 `main`)에 PR·리뷰 없이 그대로 올려버렸다(실측: `feature -> main`, git 2.43.0/Linux). 기본값 `simple` 에서는 대신 `fatal: The upstream branch ... does not match` 로 중단됐다. 이는 드문 오설정이 아니라 `git worktree add ... -b <branch>` 를 `origin/main` 에서 시작할 때의 정상 귀결이다(`branch.autoSetupMerge` 가 첫 `-u` push 전까지 시작점을 upstream 으로 잡는다). 이 상태에서는 `git status` 의 ahead/behind 가 base 기준으로 계산되므로 mispair 판정이 divergence 판정보다 먼저 실행된다. 알려진 트레이드오프: 의도적으로 다른 이름의 원격 브랜치를 추적하던 경우(`fix` → `origin/hotfix-2026-08`) 기존 추적 대상 대신 동명의 새 원격 브랜치가 생긴다 — `gh:pr` 의 역할이 "현재 브랜치로 PR 을 연다" 이므로 수용한다 (#1315)**
- 변경: **`gh:pr` 이 base 브랜치(보통 로컬 `main`) 위에서 시작된 세션을 자동 복구 — 종전에는 "피처 브랜치를 만들어 달라" 하고 멈췄지만, 이제 로컬 전용 커밋을 새 브랜치로 옮기고 진행한다. 브랜치 이름은 이 저장소의 커밋 제목이 한글이라 slugify 가 빈/깨진 결과를 내므로 ASCII 접두사만 사용한다: 이슈 번호가 있으면 `<type>/issue-<N>`, 없으면 `<type>/<YYYYMMDD>-<short-sha>`(둘 다 결정적 — `date +%s`/난수 없음). 잊기 쉬운 필수 후속 단계인 로컬 base 되감기(`git branch -f "$BASE_BRANCH" "origin/$BASE_BRANCH"`)를 함께 수행하되, `git rev-list origin/$BASE..$BASE` 가 옮겨간 커밋 집합과 정확히 일치할 때만 자동 실행하고 아니면 경고만 남긴다. 커밋이 이미 `origin/$BASE` 에 올라가 있으면 PR 가능한 상태가 아니므로 종전대로 정지한다. 되감긴 커밋은 로컬 전용이라 `reflog` 로 복구 가능하며 복구 힌트 1줄을 출력한다. 새로 만든 브랜치는 upstream 이 없으므로 위 F-1 표의 1행(`git push -u origin HEAD`)으로 자연히 착지한다. base 브랜치의 미커밋 변경(dirty worktree)은 범위 밖 — 기존 "빈 범위 → PR 할 것 없음" 조건이 덮으며 커밋은 `gh:commit` 의 몫이다 (#1315)**
- 변경: **`/devx:pr-verify-merged` 스킬 신설 — 머지된 PR 을 그 머지 커밋의 **신선한 클론**에서 다시 검증한다. `devx:pr-verify-live` 의 sister skill 로 같은 머지 후 슬롯을 맡되 증명 대상이 다르다(live=서빙 체크아웃 동일성, merged=소스 동일성). 띄울 앱이 없는 저장소(shell 스크립트 · CLI · 라이브러리)가 대상이며, 지금까지 작업 worktree 에서 하던 머지 후 검증이 놓치던 실패 클래스를 잡는다: 실측 사례에서 PR 이 추가한 픽스처의 유일한 파일이 `.gitignore` 대상이라 클론에는 케이스 디렉터리 자체가 없었고, 러너가 조용히 건너뛰어 worktree 9건 / 클론 8건으로 갈렸다(PR 은 초록으로 머지되고 리뷰어 2명도 통과시킨 상태였다). F-2/F-3 검증 전 단언(클론 HEAD == `mergeCommit.oid`, 클론 무결성 + 클론↔worktree 테스트 케이스 집합 비교)이 실패하면 측정하지 않고 정지한다 (#1312)**
- 변경: **`devx:pr-verify-merged` 의 차별 기능 2종 — (1) 환경 변이 재실행: 같은 검증을 `PATH=/usr/bin:/bin` 로 한 번 더 돌려 사용자 셸의 도구 치환을 배제한다(실측: `grep` 이 GNU grep 3.11 이 아니라 ugrep 7.5.0 이라 CRLF 처리가 기계마다 갈렸다). locale · 셸 · 줄 끝 축은 diff 에서 유도한 후보로 리포트에만 적고 `--matrix full`/`--env` 일 때만 실행하며, 축 1개 실패는 그 축만 `unverified` 로 전체 정지가 아니다. (2) 차등 검증: 주장마다 PR 이전 상태(`<merge>~1`)로 같은 입력을 돌려 전후 결과가 같으면 `unproven` 으로 표시한다 — 전후 모두 통과하는 검사는 아무것도 증명하지 않는다. 리포트는 `Clone:` `Claims:` `Matrix:` `Unproven:` `Unverified:` `Rejected:` `Created:` 를 모두 담는다 (#1312)**
- 변경: **두 pr-verify 스킬의 공통 계약을 `claude/skills/devx-pr-verify-live/references/verify-contract.md` 한 파일로 분리 — 6줄(머지 커밋 해석 · 대상 동일성 선증명 · AC 에서 주장 도출 · 주장 1건=단언 1건 · 단언 실패 시 측정 금지 · 발견은 제자리 수정 대신 이슈화)이며 양쪽 SKILL.md 가 이 파일을 가리킨다. 두 스킬은 description 에서 서로를 sister skill 로 명시한다 (#1312)**

## 2026-08-10
- 변경: **`mise run lint-sh`의 shellcheck 스캔 범위를 `bash/`에서 `shell-common/`까지 확장 — `shell-common/.shellcheckrc`(`shell=bash`)로 `#!/bin/sh` 셔뱅에서 비롯된 SC3xxx POSIX-오탐 1,242건을 걷어내자 그 아래 가려져 있던 진짜 경고 182건(79개 파일)이 드러났고 전부 정리했다. 그중 일부는 실제 버그였다: `set -e` 아래에서 이미 죽어있던 에러 핸들러(`docker_configure_proxy.sh`), `cmd && { 성공 } || { 리셋 }` 패턴이 성공 분기 내부 실패 시 값을 통째로 지워버릴 수 있던 구조(`work_log.sh` 4곳), 이미 다른 패턴에 포함돼 도달 불가능하던 case 분기(`install_claude.sh`). zsh 전용 문법(`${(%):-%N}`, `{1..$n}`)이 `shell=bash` 파서에 오탐으로 걸리는 잔여 케이스는 기존 `env/path.sh:65` 선례를 따라 `# shellcheck disable=`로 억제했다. `shfmt`는 `bash/`만 유지 — `shell-common/` 전체 재포맷은 포맷 스타일이 크게 달라 13,000줄 규모 diff가 나와 범위 밖으로 분리 (#1305)**
- 변경: **statusline 이 현재 effort 레벨을 모델 뒤에 달 위상 글리프로 표시 — `🌑 low` / `🌒 medium` / `🌓 high` / `🌔 xhigh` / `🌕 max`. Claude Code 는 statusline stdin JSON 에 `.effort.level` 을 실어 보내되 모델이 effort 를 지원할 때만 그 키를 넣는다(`claude-3-*`, `opus-4-0/4-1`, `sonnet-4-0/4-5` 등은 없음) — 키가 없으면 세그먼트와 구분자가 함께 빠져 라인이 종전과 완전히 동일하다. 매핑에 없는 값은 숨기지 않고 원문 텍스트로 보여 향후 신규 레벨이 조용히 사라지지 않게 한다. `ultracode` 는 표시하지 않는다: 레벨이 아니라 세션 모드이고 켜지면 effortValue 가 `xhigh` 로 강제되어 페이로드만으로는 둘을 구분할 수 없다 (#1302)**
- 변경: **statusline 의 jq→bash 필드 리더가 빈 필드를 잃던 버그 수정 — `@tsv` + `IFS=$'\t' read` 조합은 탭이 IFS *공백류* 문자라서 연속된 탭을 하나로 합치고 선행 탭을 버린다. 그래서 중간 필드가 비면 뒤의 값들이 통째로 왼쪽으로 밀렸다. 비는 필드(`used_pct`/`total_tokens`)가 목록 맨 끝에 몰려 있던 동안에는 후행 절삭이 무해해 드러나지 않았지만, `.effort.level` 을 6번째로 추가하면서 그 자리가 중간이 되어 실제 버그가 됐다(예: `{"cwd":"/tmp","effort":{"level":"max"}}` 가 `🧠 max` 로 모델 슬롯을 오염시키고 effort 는 사라짐). NUL 구분 출력 + `mapfile -d '' -t` 로 교체해 위치를 보존한다. 개행 구분으로 바꾸는 것만으로는 부족하다 — 값 자체가 구분자 바이트를 담으면 똑같이 뒤 필드가 밀리기 때문으로, 탭은 `.model.display_name` 에서, 개행은 `.cwd` 에서 터진다(둘 다 회귀 테스트로 고정). NUL 은 bash 변수가 담을 수 없는 유일한 바이트라 어떤 값도 위조할 수 없다. jq fork 는 여전히 1회이며, here-string 으로 `echo` 파이프의 두 번째 fork 까지 없애 렌더는 오히려 소폭 빨라졌다 (#1302)**

## 2026-08-07
- 변경: **zsh 에서 `gh-pr-review` 를 Ctrl-C 로 중단해도 호출자가 받는 종료 코드가 0 이던 문제 수정 — 이제 bash 와 동일하게 130(128+SIGINT)을 반환하므로 자동화가 인터럽트와 정상 완료를 구분할 수 있다. 원인은 trap 이 아니라 Step 5 를 감싸던 `if ! ( … | tee … ); then` 구문이었다: zsh 는 인터럽트가 *조건으로 평가되는* 서브셸 안에서 발생하면 핸들러의 `return 130` 을 버리고 해당 `if` 의 자체 상태 0 으로 빠져나간다(측정: `if ! cmd` → 0, `( … ) || rc=$?` → 0, `cmd || rc=$?` → 130, `( … )` 다음 줄 `rc=$?` → 130; bash 는 네 경우 모두 130). Step 5 서브셸을 단독 실행 후 다음 줄에서 `$?` 를 읽도록 바꾸고, trap 무장 이후의 나머지 가드도 `cmd || { … }` 형태로 통일했다(파이프라인·`pipefail`·`tee` 구조 자체는 그대로) (#1294)**
- 변경: **AI CLI 의 stderr 임시파일(`/tmp/gh-pr-review-stderr.<ai>.*`)이 Ctrl-C 시 `/tmp` 에 남던 누수 수정 — 이 경로는 `_gh_pr_review_run_ai` 의 지역변수라 바깥 `gh_pr_review` 의 INT/TERM 핸들러가 볼 수 없었다. 이제 호출자가 같은 SSOT 할당기로 미리 잡아 네 번째 인자로 넘기고 핸들러의 `rm -f` 목록에 포함시킨다. 인자를 주지 않는 직접 호출은 기존처럼 자체 할당하며, *실제 CLI 실패* 시 사용자가 들여다볼 수 있도록 파일을 남기는 동작(#694)도 그대로다 — 인터럽트 경로에서만 지운다 (#1294)**
- 변경: **`bash/main.bash`(`~/.bashrc` SSOT)에 `~/.bashrc.local` source 훅 추가, `bash/setup.sh`에 idempotent seed 로직 추가 — zsh의 `~/.zshrc.local` 격리 패턴(#737)을 bash에도 대칭 도입. gateway-migration 등 외부 installer/훅 스크립트가 `~/.bashrc`에 하드코딩 절대경로를 직접 append해 SSOT를 오염시키는 사고를 막는다. 실제 차단은 기존 pre-commit 하드코딩 home-path 가드(#1142)가 커밋 시점에 잡아내고, `~/.bashrc.local`은 그 걸린 줄을 옮겨 담는 착지 지점 역할 — zsh 쪽과 동일한 이단계 메커니즘 (#1290)**
- 변경: **zsh 에서 `my-help find`(fzf) 가 대부분의 명령어를 2줄씩 중복 표시하던 버그 수정 — dash-form 헬퍼(`git-help` 등)를 zsh 전용 진짜 함수로 새로 정의하던 `_help_std_define_zsh_dash_function` 을 제거하고, bash 와 동일하게 alias 로만 등록한다. zsh 는 alias 를 파싱 시점에 확장하므로 비대화형 호출(`zsh -c`, `setopt no_aliases`)에서 dash-form 이 안 보일 수 있어, `command_not_found_handler` 를 `*-help` → 언더스코어 헬퍼 매핑으로 일반화해 안전망으로 둔다(실함수일 때만 디스패치하며, 동명의 실행 파일이 PATH 에 있으면 그쪽이 항상 우선한다) (#1287)**
- 변경: **`devx:pr-verify-live` 가 검증 리포트를 대상 PR 에 코멘트로 게시 — Step 8 결과가 `[OK]`/`[WARN]` 이면 그 리포트 블록을 그대로 PR 코멘트로 남긴다(Step 9). `[FAIL]` 은 검증 전 단언이 막은 환경 문제이므로 게시하지 않고 로컬 출력으로만 끝낸다. 게시 경로는 `gh_pr_review.sh` 의 `_gh_pr_review_post_comment` 재사용이라 soft-fail 계약(게시 실패는 경고, 스킬 정지 아님)이 그대로 유지되고, 헬퍼는 레포 표준 폴백 블록(#644 NF-1 + #724)으로 감싸 부른다. 매 실행이 새 코멘트를 덧붙이는 append-only 동작 — 재검증 이력이 덮이지 않는다. 끄려면 새 플래그 `--no-comment`(파서 출력 `post_comment=0`); `--dry-run`/`--no-issue` 는 이슈 생성만 게이트하므로 코멘트 게시와 서로 독립이다 (#1288)**
- 변경: **`gh:pr-review` 실행 중 Ctrl-C(SIGINT)/TERM 이 들어와도 임시파일 3종(`PROMPT_FILE`/`AI_OUT`/`BODY_FILE`)이 `/tmp` 에 남지 않는다 — 기존에는 trap 이 없어 인터럽트가 함수를 통째로 되감았고, 함수 곳곳의 `rm -f` 정리 블록이 하나도 실행되지 않아 파일이 그대로 누적됐다(같은 PID 를 재사용하는 이후 실행이 자기 쓰레기 때문에 #1283 noclobber 폴백에서 막히는 2차 피해까지). 핸들러는 정리 후 `return 130` 으로 함수를 실제로 중단한다 — trap action 만 두면 bash·zsh 모두 핸들러 종료 후 *다음 명령부터 실행을 재개* 하므로, 방금 삭제한 파일로 Step 6 이 PR 코멘트를 만들어 게시하는 경로가 열린다 (#1286)**
- 변경: **trap 해제는 `trap - INT TERM` 만으로 끝내지 않고 호출자가 원래 갖고 있던 INT/TERM 핸들러를 복원한다 — bash 의 trap 은 셸 전역이라 단순 해제가 *호출자의* 핸들러까지 지워버린다(bats 자신의 `bats_interrupt_trap` 이 실제 사례). `gh_pr_review` 는 사용자의 인터랙티브 셸에 source 되므로 이 부수효과가 조용히 남는다. zsh 는 `emulate -L sh` 하에서 trap 이 함수 지역이라 복원이 자동이므로 스냅샷을 뜨지 않는다 (#1286)**
- 변경: **`_gh_pr_review_run_ai` 의 stderr 임시파일도 `_gh_pr_review_mktemp_safe` 를 거치도록 통일 — 네 번째이자 마지막 raw `mktemp` 호출 지점이었다. `/tmp` 가 쓰기 불가·쿼터 초과일 때 리뷰 전체를 중단하는 대신 안전한 폴백으로 진행하며, 그 폴백 경로에 심링크·파일이 미리 심어져 있으면 따라가지 않고 실패한다(#1283 계약 유지) (#1286)**
- 변경: **`$$` 폴백 경로 동시성 재확인 — 같은 `$$` 를 공유하는 백그라운드 서브셸 둘이 동일 폴백 경로를 두고 경쟁해도 `set -C`(O_CREAT\|O_EXCL) 가 커널 수준에서 직렬화되어 정확히 하나만 성공하고 나머지는 fail-closed 로 떨어진다(덮어쓰기·잘림 없음). 회귀 테스트로 고정했고, 통과하므로 폴백 네이밍에 `$RANDOM` 을 섞지 않고 기존 스킴을 유지한다. 아울러 지금까지 bash 로만 검증되던 noclobber/심링크 거부 동작에 zsh 자동 회귀 커버리지를 추가했다 — 이 모듈은 bash·zsh 겸용인데 zsh 경로는 수동 확인만 거친 상태였다 (#1286)**

## 2026-08-06
- 변경: **Stop 가드 두 개(`gh_issue_flow_stop_guard.py` / `devx_autopilot_stop_guard.py`)의 harness 마커 판정을 부분 문자열에서 줄머리 고정(`(?m)^`) 으로 교체 — 사람이 `"Stop hook feedback:"` 이나 `<task-notification>` 을 문장 안에 인용하기만 해도 그 턴 전체가 fresh user prompt 집계에서 빠져 stale boundary 가 만료되지 않던 문제 수정. 마커 문자열 SSOT 인 `_SKILL_EXPANSION_MARKERS` / `_HARNESS_INJECTION_MARKERS` 튜플에서 `re.escape` 로 정규식을 파생하며, 두 훅에 동일한 구조로 적용해 #1275 식 구조 드리프트를 막는다. 단 `is already loaded above` 는 예외 — 실제 주입문이 `Skill <name> is already loaded above; instructions unchanged. Arguments: …` 형태라 리터럴이 줄머리에 오지 않으므로, `_SKILL_EXPANSION_SHAPES` 의 `^Skill\s+\S+\s+is already loaded above` 정규식 조각으로 실제 문장 구조를 매칭한다(그대로 리터럴로 두면 이 한 마커에 대해 #1270 의 over-count 가 되살아난다) (#1281)**
- 변경: **`gh:pr-review` 의 임시파일 3종(`PROMPT_FILE`/`AI_OUT`/`BODY_FILE`) 할당을 `_gh_pr_review_mktemp_safe <template>` 하나로 통일 — `mktemp` 실패 시 쓰던 예측 가능한 `$$` 폴백 경로를 `set -C`(noclobber, O_CREAT\|O_EXCL) 서브셸로 배타 생성하고, 그 자리에 파일이나 심볼릭 링크가 이미 있으면 링크를 따라 덮어쓰지 않고 즉시 실패한다. 세 호출 지점 모두 실패를 검사해 `gh_pr_review` 를 중단하므로(빈 경로로 진행하지 않음), `/tmp` 가 쓰기 불가·쿼터 초과일 때 로컬 공격자가 PID 를 예측해 심링크를 심어 임의 파일을 덮어쓰던 경로가 닫힌다 (#1283)**
- 변경: **`gh:pr-review` 의 `PROMPT_FILE` 경로를 레인별로 분리 — `_gh_pr_review_mktemp_prompt <ai> <PR#>`(`mktemp "/tmp/gh-pr-review-prompt.<ai>.<PR#>.XXXXXX"`) 가 SSOT. `devx:pr-review-all` 이 agy·codex 레인을 같은 턴에 병렬로 띄울 때 두 레인이 고정된 프롬프트 파일 이름을 공유해 서로 덮어쓰면 두 CLI 가 동일한 바이트를 리뷰하고도 `agy:OK codex:OK` 로 조용히 통과하던 위양성을 차단한다. SKILL.md Step 4 는 프롬프트 작성과 Step 5 디스패치를 같은 Bash 호출 안에서 수행하도록 명시(Bash 툴은 호출마다 새 서브프로세스라 `$PROMPT_FILE` 이 다음 호출로 이어지지 않는다) (#1276)**
- 변경: **`PostToolUse:Bash` 훅 지연 제거 — `gh pr create` 후 project board retry-poll(최대 6회 x 2s sleep + GraphQL 왕복)과 연결 이슈 동기화를 백그라운드로 분리. foreground 는 sync 1회만 수행하므로 훅이 재시도 예산을 기다리지 않고 반환한다 (median 7.8s / max 48.7s → sync 1회 왕복) (#1258)**
- 변경: **훅 구간별 타이밍 계측 추가 — 라우팅된 호출만 `~/.local/state/claude/post-bash-dispatch-timing.log` 에 기록하고, `claude/tools/hook-perf-report.sh` 로 median/p90/p99/max 와 목표치(median < 2000ms · p99 < 5000ms) PASS/FAIL 을 집계한다 (#1258)**
- 변경: **커맨드별 마크다운 레퍼런스 자동생성 — `shell-common/tools/custom/gen_command_docs.sh` 가 `_<topic>_help_rows_<section>()` row 함수를 실행해 `docs/guide/commands/<커맨드>.md` 57개를 생성. `rg "<키워드>" docs/guide/commands/` 로 커맨드 동작 전체 텍스트 검색 (#1262)**
- 변경: **호스트 환경값(`$SSL_CERT_FILE` 등)을 출력하는 `ssl`/`crt` topic 은 문서 생성에서 제외 — 머신마다 값이 달라 재현 불가능하고 사내 인증서 경로가 공개 저장소로 새어 나간다. 해당 값은 `ssl-help`/`crt-help` 로 직접 확인 (#1262)**
- 변경: **소스 주석에만 있던 엣지케이스를 `docs/guide/commands/.notes/<커맨드>.md` 에 수기 보강하면 재생성 시 문서에 삽입 — 첫 대상은 `csm find` 의 fzf 피커/Esc 무음 종료 동작**
- 변경: **`my-help search`(alias `find`) 인덱스를 `*_help.sh` 토픽에서 저장소 전체 alias까지 확장 — alias 이름으로 바로 찾고, 고르면 정의·정의 위치·주석을 표시. 토픽을 그대로 다시 노출하는 dash-form alias(`agy-help` → `agy_help`)는 중복 제거하되, 서로 다른 정의를 가진 동명 alias(`llm-help`)는 양쪽 다 노출한다 (#1261)**
- 변경: **alias 인덱스는 `${XDG_CACHE_HOME:-~/.cache}/dotfiles/my-help-alias-index.tsv`에 24시간 TTL로 캐시 — `MY_HELP_ALIAS_CACHE_PATH` / `MY_HELP_ALIAS_CACHE_MAX_AGE`로 재정의 가능하며, 캐시가 비었거나 손상되면 자동 재생성**
- 변경: **gh-issue-flow Stop 훅이 `Bash` heredoc/`printf` 로 출력된 Step 3 종료 리포트도 인식 — 기존에는 assistant text 블록만 스캔해 리포트가 tool_use `input.command` 와 `tool_result` 에만 남으면 플로우가 영원히 종료되지 않았다. 템플릿 자리표시자(`<N>`/`<i>`) 대신 실제 숫자를 요구하는 엄격한 정규식을 쓰고 `Bash` 만 스캔한다(`Write`/`Edit` 입력은 SKILL.md 편집 시 진짜 템플릿 텍스트를 담으므로 제외) (#1270)**
- 변경: **오래된 flow boundary 자동 만료 — 종료 리포트 없이 끝난 플로우의 boundary 가 세션 내내 남아 무관한 턴까지 계속 차단하던 문제 수정. boundary 이후 사용자 프롬프트가 3회 쌓이면 fail-open 한다. `GH_ISSUE_FLOW_STOP_GUARD_MAX_USER_TURNS` 로 조정하고 `0` 이면 만료 비활성화. tool_result·skill 확장·`<system-reminder>` 전용 메시지는 사용자 프롬프트로 세지 않는다 (#1270)**
- 변경: **Step 3 리포트는 반드시 plain assistant text 로 출력하도록 SKILL.md·report-template.md 에 명시 — `Bash` 경로는 어디까지나 폴백 (#1270)**
- 변경: **flow boundary 만료 카운터가 훅 자체 피드백을 사용자 턴으로 오인하던 문제 수정 — 실제 2489-entry 트랜스크립트에서 "새 사용자 프롬프트" 102건 중 실제 사람 입력은 4건뿐이었고, 나머지는 훅이 되돌려받은 `Stop hook feedback:` 62건과 백그라운드 서브에이전트 완료 알림 `<task-notification>` 40건이었다. 그 결과 1/6 단계에서 가드가 스스로 꺼졌다(`devx:pr-review-all` 이 백그라운드 에이전트 3개를 띄우므로 상시 재현). 이제 트랜스크립트 엔트리의 `isMeta` 플래그와 harness 주입 마커를 걸러내고, 반대로 사람이 쓴 텍스트가 `tool_result` 와 같은 턴에 실려 온 경우는 정상적으로 사용자 턴으로 센다 (#1270)**
- 변경: **`Bash` 폴백 종료 채널을 command + `tool_result` 쌍 매칭으로 좁힘 — 기존에는 `Bash` 명령 문자열에 종료 마커가 들어 있기만 하면 플로우를 종료로 판정해, `cat <<'EOF' > /tmp/report.txt` 처럼 파일로 리다이렉트하거나 셸 주석에 마커가 있는 경우에도 리포트를 실제로 출력한 것으로 오인했다. 이제 tool_use 의 `input.command` 와 같은 `id` 를 가진 `tool_result` 가 **둘 다** 엄격 정규식에 매치할 때만 종료로 본다(리다이렉트는 stdout 이 없어 쌍이 성립하지 않음). SKILL.md 를 읽어 `tool_result` 에만 템플릿이 실리는 #608 경로는 명령 쪽(`Read`/`cat`)이 숫자 형식을 만족할 수 없어 여전히 차단된다 — 쌍 조건은 양쪽 각각보다 엄격하다 (#1270 / PR #1272 리뷰)**
- 변경: **`/devx:pr-verify-live` 스킬 신설 — 머지 직후(또는 PR 브랜치에서) 이미 떠 있는 dev 앱에 붙어 PR 이 실제로 동작하는지 확인하고 발견을 대상 프로젝트 레포 이슈로 등록한다. base URL·API origin 을 하드코딩 없이 발견하고, 서빙 중인 체크아웃이 대상 커밋(`mergeCommit.oid`)을 포함하는지 확인한 뒤에만 측정한다 (#1271)**
- 변경: **`devx:pr-verify-live` 의 검증 전 단언 3종 — 서빙 체크아웃 · 로케일/뷰포트 전환 적용 · 대상 요소 비가림. 하나라도 실패하면 측정하지 않고 정지한다. "화면은 멀쩡히 뜨는데 검증이 무효" 인 실패를 여섯 번의 실행에서 반복 관측한 결과 (#1271)**
- 변경: **`devx:pr-verify-live` 는 이슈 등록 전에 자기 반증 3가설(하네스 오류 · 데이터 상태 · 의도된 동작)을 세워 위양성을 걷어낸다. 근거 게이트는 "수치" 가 아니라 "기계 판독 가능한 단언" 이라 i18n·상태·어포던스 결함도 등록 가능하다. 이슈 본문·라벨·메트릭은 `gh:issue-create` 가 SSOT (#1271)**
- 변경: **devx-autopilot Stop 훅에도 오래된 boundary 자동 만료 이식 — 종료 리포트 없이 중단된 Stage-B 실행의 boundary 가 세션 내내 남아 무관한 턴까지 계속 차단하던 문제 수정(gh-issue-flow 는 #1270 에서 이미 해결). boundary 이후 진짜 사용자 프롬프트가 3회 쌓이면 fail-open 하며 `DEVX_AUTOPILOT_STOP_GUARD_MAX_USER_TURNS` 로 조정하고 `0` 이면 만료 비활성화. 엔트리의 `isMeta` 플래그, 훅 자신의 `devx-autopilot incomplete:` 재주입, `<task-notification>` 등 harness 주입, skill 확장, `<system-reminder>` 전용·tool_result 전용 메시지는 사용자 프롬프트로 세지 않는다 (#1275)**
- 변경: **`Bash` 폴백 종료 채널이 리포트 *전체 형태* 를 요구하도록 추가로 좁힘 — `grep "gh:issue-flow complete (#1270)" some.log` 는 명령 문자열에 숫자 형식 마커가 있고 grep 이 그 줄을 그대로 stdout 으로 되돌려주므로 쌍 매칭 두 조건을 모두 만족했다. 즉 진행 중인 flow 와 같은 이슈 번호를 로그에서 검색하기만 해도 플로우가 조기 종료될 수 있었다. 이제 짝지어진 `tool_result` 가 마커뿐 아니라 리포트 필드 줄(`PR URL:` 성공형 / `Resume after fix:` 실패형)까지 담아야 종료로 본다 — 한 줄짜리 grep 출력으로는 재현되지 않는 구조다. 명령 쪽 조건은 그대로 두어 heredoc 리포트는 계속 인식되며, `tool_result.content` 가 텍스트 블록 리스트로 쪼개져 오는 경우를 위해 매칭 전 블록을 합친다. 필드 이름이 report-template.md 에서 바뀌면 훅이 조용히 무력화되지 않도록 드리프트 테스트를 함께 추가했다 (#1274)**
- 변경: **macOS/BSD 이식성 수정 — 타이밍 테스트가 GNU 전용 `date +%s%N` 대신 bash 내장 `SECONDS` 를 쓰도록 바꿔 BSD `date` 에서 테스트가 중단되지 않는다. ms 변환 헬퍼는 `claude/hooks/lib/pbd_ms.sh`(디스패처와 같은 디렉터리에 함께 배포되어야 하는 신규 파일) 로 분리해 BSD `date` fallback 경로에 테스트 커버리지를 붙였다 (#1258 리뷰 후속)**

## 2026-07-30
- 변경: **statusline 시간 표시 `HH:MM:SS`로 축약 + fcitx 한글/영어 입력기 상태 표시 (#1251)**
- 변경: **`CLAUDE_STATUSLINE_SKIP_FCITX=1` 환경변수 추가 — Windows Terminal/VS Code Remote-WSL처럼 X11 클라이언트가 아닌 콘솔에서는 fcitx 상태를 원천적으로 못 읽으므로 매 프롬프트 fcitx-remote 호출을 건너뜀 (개인 PC는 `~/.zshrc.local`에서 설정)**
- 변경: **`claude-skills-marketplace search`(alias `find`)에 fzf 퍼지 피커 추가 — fzf 미설치·비대화형이면 기존 jq substring 검색으로 폴백**

## 2026-07-29
- 변경: **`my-help search` (alias `find`) 추가 — fzf 퍼지 topic 검색, fzf 미설치·비대화형이면 카테고리 목록 폴백 (#1246)**

## 2026-07-24
- 변경: **changelog 신설 — 일일/주간 보고 허브(my-share) 수집 대상**
- 변경: **`herdr-help install` 섹션 추가 — 사외 표준 설치 + 사내 프록시 우회 설치 안내 (#1239)**
